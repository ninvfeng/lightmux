# lightmux · PRD

> **一句话**：一个**轻量极致、为 tmux 而生**的 Android SSH 客户端。
> 整包 3.6MB，主页就是 tmux 会话切换器——列表展开即会话，一次点击直达窗口；
> 除核心 SSH 外还带 SFTP 文件管理、主机概览、端口转发与内置预览，以及可自定义的快捷栏。

- **仓库**：`cnb.cool/ninvfeng/lighttools/lightmux.git`（主）+ GitHub `ninvfeng/lightmux`（开源门面）
- **包名**：`net.lighttools.lightmux`
- **许可**：MIT（vendored 终端内核保持 Apache-2.0，见 §3）
- **状态**：v0.1.47 · 已发布，持续迭代中
- **文档版本**：2026-08-22
- **实现与本文的偏差**：见文末「实现与本文的偏差记录」

---

## 1. 为什么做这个

手机上用 SSH 的人，真实工作流几乎都是 tmux：连上去 → `tmux ls` → `tmux a -t xxx` → 找窗口。
现有 Android SSH 客户端普遍把 tmux 当**终端里的一个功能**——先进终端，再开面板，再选会话。
这在桌面没问题，在手机上是三次导航换一次上下文切换。

lightmux 的赌注是把这个层级**倒过来**：tmux 会话不是终端的附属品，它就是你要去的地方。
主页直接列出「主机 → tmux 会话 → 窗口」，展开、点击、进去。终端是结果，不是入口。

这个定位同时决定了取舍：**只做 SSH，不做 mosh**；**优先服务已经在用 tmux 的人**，不教育新用户。

### 与常见做法的差异

| | 常见 Android SSH 客户端 | lightmux |
|---|---|---|
| 主页 | 主机列表（连接管理） | **tmux 会话切换器** |
| tmux | 终端页里的一个面板 | 一等公民，主页即面板 |
| 导航 | 底部多 Tab + 终端页侧滑抽屉 | **无导航栏**，单列表页 + 顶栏设置图标；侧滑抽屉里装的就是这个列表页本身 |
| 体积 | 通常十几到几十 MB | **3.6MB** |
| 协议 | SSH + mosh | 纯 SSH |
| 许可 | 多为闭源或 source-available | MIT 开源 |
| 定位 | 移动工作台（全能） | tmux 快切器 + 够用的周边（专精） |

---

## 2. 代码来源

**app 层 100% 从零实现**。交互设计、信息架构、踩坑得来的**事实结论**（如「tmux 用 `:` 分隔因为
TAB 会被替换」）都可以借鉴，代码不拷。

`terminal-emulator` / `terminal-view` 共 42 个 Java 文件 / 1.1 万行，从
[termux/termux-app](https://github.com/termux/termux-app) 上游**直接取**（不取任何第三方改造版，
那部分版权归属不清），保留原始版权头与 NOTICE。上游 `TerminalSession` 通过 JNI `forkpty`
绑定本地 PTY，网络终端用不了；lightmux 做了一次「传输无关」改造（删 JNI、引入 `TerminalTransport`
接口），改动处标注 `[lightmux]`。

---

## 3. 许可

MIT。vendored 的两个模块继续以 Apache-2.0 分发（保留版权声明、`NOTICE`、目录内 LICENSE 全文）——
Apache-2.0 代码可以放进 MIT 项目，整包分发时用户受两份许可约束。

依赖侧：sshj / AndroidX / Compose / Kotlin 是 Apache-2.0，Bouncy Castle 是 MIT-style，
JetBrains Mono 是 OFL 1.1，**零 GPL / LGPL / AGPL**。完整清单见 `THIRD_PARTY_NOTICES.md`，
可用 `./gradlew app:dependencies` 复核。

唯一被许可挡在门外的是 **mosh**（GPLv3）：常见规避手法是「独立二进制 + 子进程 IPC」绕开链接传染，
法理上站得住，但分发含它的 APK 仍须随附对应源码，对一个想被随便用的 MIT 项目是持续负担。
`TerminalTransport` 留了扩展位，真要做也是**独立可选下载**，不进主 APK。

交付物均已就位：`LICENSE`、`NOTICE`、两个模块目录内的 Apache-2.0 全文、`THIRD_PARTY_NOTICES.md`、
README 的「许可」段、应用内「关于 → 开源许可」页、`CONTRIBUTING.md`。

### 字体策略

> **现按本节设计内置 JetBrains Mono Regular**（原样打包，未子集化）。中间一度收窄到「连字体也不打包、
> 直接用系统等宽」，后又改回——系统等宽（Droid Sans Mono）不含制表符，TUI 框线会跨字体回退且被渲染器
> 拉伸变形。详见文末「实现与本文的偏差记录」。

同类产品常见的做法是内置多套字体再支持运行期下载，光字体就能占掉大半个 APK。
lightmux **收窄到一种**：

- **内置 JetBrains Mono Regular**（OFL 1.1）作等宽主字体，268KB。
- **CJK 走系统回退**——Android 自带 Noto Sans CJK，终端里中文能显示，代价是中英非严格 2:1 等宽。
- **不做**字体下载中心、不做子集化、不做 maple 变体。

理由是体积和维护面：整包要压在个位数 MB 以内，字体管理模块（下载中心 + 外观页的字体分区）
少说几百行。用户要专属字体 → V2 做「导入本地 TTF」，用户自带文件。

---

## 4. 信息架构（核心设计）

### 4.1 单页架构：全应用只有一个列表

**没有底部导航栏，没有独立的会话页。** 主页是唯一的常驻页面，其余全是它的二级页。

```
┌──────────────────────────────────────────────┐
│ lightmux                            ⟳    ⚙   │  顶栏：刷新 · 设置
├──────────────────────────────────────────────┤
│ 已连接                      断开不影响远端 tmux │
│ ⛁ gz3: main         gz3                   ⛓  │
│ ▣ zsh               hk · 连接中…           ⛓  │
├──────────────────────────────────────────────┤
│ ▾ gz3   8.138.125.201  ●2   [终端][监控][文件] │
│    ├ ▾ ⛁ main          2 窗口 · 已附加         │
│    │     ├ 0: server   ● active               │
│    │     └ 1: logs                            │
│    ├ ▸ ⛁ deploy        1 窗口                 │
│    └ ○ zsh             ● 活动 · 12ms          │  ← 裸会话（非 tmux）
│ ▸ hk    nanogptproxy        [终端][监控][文件] │
│ ▸ fnos  192.168.0.241       [终端][监控][文件] │
│                                        ( + ) │  FAB：新增主机
└──────────────────────────────────────────────┘
```

- **顶部「已连接」段**（0.1.63 加入）：当前活着的会话——已 attach 的 tmux 会话与裸会话一起——
  拉平成一张清单摆在树上方。点击回到那个终端，行尾 `⛓` **断开这条 SSH，远端 tmux 会话照跑**，
  回头点树里那个会话行就 attach 回去。没有会话时整段不出现，不给常态多占一行。
  图标用断链不用叉：树里三处 `✕` 的含义都是「杀掉」（kill-session / kill-window / 关裸会话），
  这里恰恰不是，也因此不设二次确认。它不是第二份事实来源，读的就是 `SessionManager` 那条流，
  和树里那些行是同一批 handle（详见 §4.2 的决策修订）。
- **第 1 级 主机**：点击行体展开/折叠。右侧四个图标入口——**终端 / 监控 / 文件 / 转发**
  （前三个是最初就定下的形态；转发 0.1.31 补上，理由见文末偏差记录）。
  有活动会话时行尾打 `●N` 标记。长按 → 编辑 / 复制 / 删除。
- **第 2 级 tmux 会话**：点击 = attach（存在则附加，否则新建）；长按 = 重命名 / detach。
  行尾显示窗口数与附加状态，右侧两个图标——展开窗口 `⌄` 与结束会话 `✕`（二次确认）。
  **这一级是本产品的存在理由，交互必须最短路径。**
- **第 3 级 窗口**：点击 = `select-window` 后进入终端。显示 index + 名称 + active 标记。
  行尾 `✕` = 关闭窗口（`kill-window -t @id`，二次确认；关掉最后一个窗口连会话一起结束，是 tmux 的语义，不拦）。
  **关和切都用 `@id` 不用 index**——index 会随窗口增删往前挪，拿它去 kill 就是在关隔壁那个。
- **关闭一律摆在行尾图标上，不藏进长按**：裸会话、tmux 会话、窗口三级的 `✕` 对齐成一列，
  扫一眼就知道每行怎么关。长按菜单只留没有图标位的低频项（重命名、detach）。
- **新建**：主机展开后末尾两行——「新建 tmux 会话」（填个名字，走 `new-session -A`，
  和 attach 同一条命令）与「新建终端」（裸 SSH）。**tmux 那条排在前面**：会话活得比连接久
  是这个产品的全部价值，把它排在裸终端后面等于把卖点藏起来。窗口列表末尾同理有「新建窗口」，
  已 attach 走侧通道 `new-window`，没 attach 则与 attach 合成一条登录命令。
- **没装 tmux 的主机**：探测时顺带认一下包管理器（`apt-get`/`dnf`/`yum`/`pacman`/`apk`/`zypper`/`brew`/`pkg`）
  与当前 uid，状态行变成「点击安装」。**要执行的命令原样摆在确认框里**——这是代用户改动他的系统，
  批准之前得看得见批准的是什么。确认后**在新终端里跑**，不走侧通道：sudo 要密码、包管理器会弹询问、
  `apt-get update` 在慢机器上远超侧通道超时，而且这条命令的每一行输出都该让人看见。
  认不出包管理器就仍然只是一句「没装 tmux」，不摆一个点了没反应的按钮。
- **裸会话**：本 app 开的非 tmux 会话与 tmux 会话平级列在主机下，行尾显示延迟。长按 = 改标题 / 关闭。

**顶栏图标**：`⟳` 刷新已展开的主机（等价于下拉刷新，给不习惯下拉的人一个显式入口）、`⚙` 设置（二级页）。
有活动会话时刷新左边多一个 `⛓` 全部断开——和通知栏那个动作同一件事：**只关本地会话，不碰传输队列**，
服务器上的 tmux 会话照跑。没有会话就不显示，插在刷新左边所以出现与消失都不会挪动另外两个图标的位置。
挨着刷新，误触代价大，二次确认。

### 4.2 为什么砍掉导航栏与会话页

三件事本来就是同一件事，拆开反而制造了「同一个会话在两个地方出现」的一致性问题：

| 常见做法 | lightmux | 理由 |
|---|---|---|
| 底部 Tab「会话」列出所有活动会话 | 裸会话直接挂在主机节点下 | 会话天然属于某台主机，独立成页等于把树拍平再让用户自己拼回去 |
| 终端页侧滑「快速切换抽屉」（主机→会话→窗口三级树） | **主页就是那个抽屉**——字面意义上的同一个 Composable（V2 修订，见下） | 抽屉里的树和主页的树完全同构，留一份实现就够 |
| 底部 Tab「设置」 | 顶栏齿轮图标 | 设置是低频入口，占 1/3 导航宽度不划算 |

**代价说清楚**：终端 → 终端的切换比「侧滑抽屉直达」多一步，lightmux 要先返回主页。
接受这个代价，换来的是**唯一事实来源**——会话状态只有一处渲染、一处维护，不会出现抽屉里显示已断开、
会话页显示还活着这类分裂。且返回键本来就是 Android 用户的肌肉记忆，路径是 `返回 → 点击`，两次操作。

#### 决策修订（V2，0.1.3）：抽屉加回来，装的就是主页

实测下来切换确实频繁，代价比预估的大：从终端切到同一台机器的另一个窗口要走「返回 → 展开 → 点击」三步，
而这是全 app 最高频的一条路径。原方案预留的「横向会话胶囊条」解决不了这条——它只列会话，不列窗口。

**上面那条反对意见的实质是「同一份会话状态两处渲染、两处维护会分裂」，而它在实现层可以彻底消掉**：
抽屉里放的**就是 `HomeScreen` 本身**，不是另写一棵同构的树。`HomeViewModel` 挂在 Activity 的
ViewModelStore 上，两处拿到同一个实例、同一份 `tmuxStates`——一个 Composable、一份状态、一份数据。

0.1.3 曾按「只读切换视图」实现（无长按菜单、无新建会话，另一套行组件、另一份展开集合），
0.1.7 撤掉了：那等于每加一个交互都要抄一遍，「加载中 / 读取失败 / 没装 tmux / 没有会话」
这四个分支只要有一处漏掉一个，就会长成「用户的会话没了」。剩下三条约束不变：

1. **零额外拨号**（见 §4.3）：拉开抽屉只对当前主机静默刷新，且只肯蹭现成的连接，蹭不到就显示带
   时间戳的缓存。为渲染一个抽屉去握手认证，等于把「绝不自动全量探测」悄悄作废。
2. **不给主页和文件编辑器**。主页本身就是这棵树，抽屉里那份连组合都不组合（两个 `LazyColumn`
   共用一个 `listState` 会互相把对方滚飞）；编辑器有未保存改动，抽屉一点就整栈替换，确认框没机会弹。
3. **返回键仍然只有一个入口**：抽屉那一段写在 `LightmuxRoot` 的中央 `BackHandler` 里，
   抽屉面板本身用不带 `drawerState` 的 `ModalDrawerSheet` 重载，避开 M3 自带的返回处理。

作用范围：终端 / 监控 / 文件三页，左边缘右滑拉出。已 attach 的会话切窗口走侧通道 `select-window`，
终端原地换窗口，**不重建、不重连、滚屏历史保留**。

### 4.3 冷启动难题与解法（关键技术决策）

**问题**：主页要显示 tmux 会话，但列 tmux 必须先有 SSH 连接。冷启动时一台连接都没有，
如果为了渲染主页去拨所有主机的连接，会是灾难（耗电、慢、可能触发对端 fail2ban）。

**解法：缓存优先 + 懒探测。**

1. **列表默认渲染缓存**：上次探测到的会话/窗口结构持久化在本地，冷启动直接显示，
   带**灰色时间戳**（「3 分钟前」）明示这是快照而非实时。
2. **展开即探测**：用户点开某台主机 → 才拨连接 → `exec` 侧通道拉一次结构 → 刷新该行并更新缓存。
3. **已有活连接的主机**：复用该连接的 `exec` 侧通道，不新拨。
4. **点击即校准**：从缓存点进一个会话，attach 用的是 `new-session -A -s <name>`（原子「存在则附加否则创建」），
   即使缓存过期也不会点空——最坏情况是新建了一个同名会话，而非报错。
5. **绝不自动全量探测**。手动下拉刷新 = 探测所有**已展开**的主机。

> 缓存以**会话名**为身份键，不用 `$id`（tmux server 重启即失效）。

### 4.4 保留的三个入口

- **监控页**：发行版 / 内核 / 运行时长 / 负载 / CPU（总体 + 每核）/ GPU / 内存 / 磁盘 / 网卡 / 进程 Top。
  一次 `exec` 拿全部，分段哨兵切分，**采不到的项如实标「不可用」，不拿 0 充数**。
  以直读 `/proc` 为主，不依赖 `top`/`free` 这类输出因发行版而异的工具；**首版只支持 Linux**，
  没有 `/proc/stat` 的主机（macOS/BSD）直接显示「暂不支持」，不做 sysctl 兼容。
  **停留在页面时每 5 秒刷新一次，离开立即停**——CPU 使用率和网卡速率本来就得靠两次采样求差，
  一次性快照给不出这两个数；代价是必须保证轮询随页面生命周期停掉，否则后台狂发 SSH 既费电又踩 fail2ban。
  **容器列表**（0.1.8 加入）：同一条 exec 里顺带 `docker ps -a`（没有 docker 就试 podman），
  列名称 / 镜像 / 状态，运行中的排在前面。不采 CPU 内存——那要 `docker stats`，得再停一秒。
  原本担心「多数主机上只会给出一片报错」，实际上不成立：采不到（没装 / 当前用户不在 docker 组）
  就整段不显示，和 busybox 采不到 `ps` 是同一个处理，不会给用户看任何报错。
  命令套 `timeout`，卡死的 dockerd 拖不垮整页监控。
  **GPU**（0.1.68 加入）：同一条 exec 里顺带探一次——有 `nvidia-smi` 就问驱动要占用 / 显存 / 温度，
  没有就退到 AMD 的 `/sys/class/drm/card[0-9]/device/gpu_busy_percent`（glob 不能用 `card*`：
  `card0-DP-1` 这类连接器目录会让同一块卡列好几遍）。探不到就整段不显示，同容器——
  没有独显的机器上这一段不占任何位置。不做 Intel 核显：那要 `intel_gpu_top` 且得有 root。
  单项读不出来（直通给虚拟机的卡报不出 utilization）时那一条进度条整条不画，**不补 0**：
  0% 会被当成「这张卡闲着」，而它可能正被另一台虚拟机跑满。
- **文件管理（SFTP）**：复用同一条 SSH 连接的浏览器——浏览 / 上传 / 下载 / 新建目录 / 重命名 / 删除 /
  小文本文件在 app 内编辑保存。传输走 Application 级后台队列，进度可见可取消。
- **端口转发**（0.1.30 加入，原在 V2）：等价于 `ssh -L`，服务端起在 `127.0.0.1` 上的服务
  映射到手机本地端口，浏览器里直接开。**只绑环回口**——绑 `0.0.0.0` 等于用户连一次咖啡店热点
  就把内网服务开给同网段，而他完全不会意识到；要开放得是显式选项，不能是默认。
  **打开转发就扫一次远端监听端口**（0.1.38 改；原先要再点一下放大镜）：点开「转发」这一下
  就是意图，等同 §4.3 的「展开即探测」，而 §4.3 要避的是替所有主机批量拨号，不是这一次
  针对单台的 `ss -tlnp`。只监听环回口的排在前面——那些正是非转发不可的。
  端口多起来（一台开发机二三十个监听是常事）靠**搜索框过滤**已经拿回来的那份列表，
  按端口号 / 进程名 / 监听地址就地筛，不再发起第二次请求。
  转发自己管连接（同 SFTP，理由见 §6.2），断线按退避重连，
  活着的转发算进前台服务的存活条件。
- **内置迷你浏览器**（0.1.36 加入）：转发出来的地址在 app 内打开，而不是甩给系统浏览器。
  理由不是「体验统一」，是**转发本身**——隧道靠本 app 的进程维持，切去浏览器等于自己退到后台，
  而那正是省电策略掐掉后台联网的时机（见下面那条）：用户看到的是一个刚点开就断掉的页面。
  留在前台，隧道就不会当着面断。
  功能到「看一眼转发出来的页面」为止：地址栏（纯数字当本机端口，`3000` = `127.0.0.1:3000`）、
  返回键后退、刷新、进度条。**没有标签页 / 书签 / 历史库**——那些都要落盘、要 UI、要迁移，
  而这里的用法是「点开、看完、退出」。用系统 `WebView`，**零新依赖**，安装包只多出这几个类。
  外部浏览器那条路没删，收进了溢出菜单。
  操作栏在**底部**且压到 44dp（0.1.37 调整）：手机上要看的是页面本身，一条 64dp 的标题栏
  在小屏上换掉的是能看见的内容；栏里只显示地址不显示网页标题——这个浏览器是给转发端口用的，
  「开着的是哪个口」比「页面叫什么」有用。
  **它是终端右边的一层「后台页」，不是导航栈里的一页**（0.1.39 改）：终端上从右往左划把它拉出来，
  网页上从左往右划把它收回去，收回去时 **WebView 不销毁**。进栈的页面一返回就没了，
  登录态、滚动位置、正在跑的 SPA 全部从头再来，而这个浏览器的用法恰恰是
  「看一眼页面、回终端敲一行、再看一眼」——每次都重开一遍等于让这条路不可用。
  同一时刻只留一页（再开一个端口就换掉它），操作栏的「×」才是真关掉。
  终端上还没有后台网页时，同一个手势掀的是转发面板：那是同一个意图的前一半——
  「刚起来的服务，我要看看」，有网页时它就是答案，没有时下一步必然是去配转发。
  代价是要给环回口开明文放行（`network_security_config.xml`），只放行 127.0.0.1 与 localhost：
  流量没出过这台设备，不存在可被中间人窃听的路径。
- **终端里自动认出端口**（0.1.34 加入）：`npm run dev` 打出 `http://localhost:5173/` 的那一刻，
  终端底部就浮一条「转发它」——**用户不必知道 lightmux 有转发功能，也不必记住端口号**。
  形态对标 VS Code 的端口自动检测：检测 → 提示 → 一点建好 → 通了再给「打开」。
  数据源是 emulator 已经解析好的屏幕文本（含 40 行滚屏），不碰原始字节流；
  由屏幕更新驱动、每秒最多扫一次，终端静止时一次都不扫。
  **宁可漏报不可误报**：只认 `localhost`/IPv4/方括号 IPv6，不认域名，同端口只提一条且环回优先
  （vite 的 Local/Network 是同一个服务），配过的端口不再提，「×」过的端口本次运行不再提。
- **后台联网受限的引导**（0.1.33 加入）：**前台服务保得住进程，保不住网络**。
  国产 ROM 的省电策略会在应用切后台几秒内掐掉它的后台联网，而转发的使用姿势恰恰就是
  「转完切去浏览器」——用户看到的是一个刚打开就断掉的页面。这件事没有代码层的解法，
  能不能在后台联网由系统策略说了算。所以 app 只做一件事：**把授权入口递到用户面前**。
  转发页在受限时挂一条提示条（授权完自动消失），设置 › 通用留一个长期入口显示当前状态。
  各家 ROM 把这个开关藏在完全不同的菜单里（「完全后台行为」「省电策略」「应用启动管理」），
  标准 Intent 拉不起来就退到应用详情页——多点两下，总好过点了没反应。

---

## 5. 功能范围

### V1（v0.1.0，首版）

**连接管理**
- 主机增删改：名称、host、port、user；密码 / 私钥（PEM）认证
- 密钥库（设置 › 密钥管理，主机表单的私钥来源行也有入口）：私钥可从本地文件导入，导入一次多台主机共用；
  主机只存一个 keyId，改钥匙所有引用它的主机跟着变
- 凭据经 Android Keystore（AES-GCM）加密存储，不落明文
- 主机密钥 TOFU 校验（首次记录，变更告警）
- 登录后自动执行命令
- 跳板机（`ssh -J`）：指向主机表里另一台主机，最多 5 跳（0.1.56 加入，见文末偏差记录）

**tmux 切换器（主页）**
- 三级树展开、缓存优先渲染、懒探测（见 §4）
- 会话：attach / 新建 / 重命名 / detach / kill
- 窗口：列出 / 切换
- 全部动作走 `exec()` 侧通道，**绝不向前台终端注入按键**

**终端**
- SSH 会话，多会话常驻（Application 级 SessionManager，不随导航销毁）
- 前台服务保活；断线自动重连（指数退避 + 网络恢复立即重试）；重连后自动 attach 回原 tmux 会话
- 复制 / 粘贴、捏合缩放、滚屏历史
- **会话管理没有独立页面**：改标题 / 关闭 / 复制会话全部在主页树里完成——关闭走行尾图标，其余走长按（见 §4.2）；
  0.1.63 起主页树上方另有一段「已连接」清单，用来一眼看清还连着什么、就地断开（仍不是独立页）
- **快捷栏**：返回 · 命令 · 文件 · 转发 · `Ent` `Esc` `↑↓←→` `Ctrl` `Tab` `Alt` `-` `|` `~` `/`，
  顺序与内容可自定义（另有 `Home` `End` `PgUp` `PgDn` 一批与 `^C` `^D` `^Z` `^B` 等组合键可选），
  键位大小用滑块连续可调（设置 → 外观）。
  也可**自己造键**：一格发一串文本（可选自动回车），或发一串**按键序列**——
  `C-b d`、`C-b 1`、`M-.`、`F5`、`Up Up Enter`（0.1.58）。预设组合键只覆盖 `^X` 单击，
  而 tmux 用户真正想顶到栏上的是带前缀的两击，序列模式一格发完。
  序列**点选**而不是手写：Ctrl / Alt / Shift 三个开关（语义同粘滞修饰键，加完一键自动清），
  具名键与 F 键排成键帽点一下加进去，字母符号走一个单字符框（0.1.59）；
  tmux 记法只是落盘格式（`KeyNotation.format` / `parse` 互逆），用户不必学它
  > 这一条虽属「终端体验增强」，但在 V1 是**必需**：没有 `Ctrl` 键，Android 软键盘打不出 `Ctrl-B`，
  > 整个 tmux 前缀键失效，主打功能直接不可用。故划入核心。
  > 终端页**没有顶栏**，返回就是栏上第一格（0.1.9，见文末偏差记录）。
- **快捷命令表**（栏上「命令」那一格弹出）：条目可增删改，也可调顺序——
  新命令一律追加在末尾，没有排序就只能删了重加，而重加又落到末尾
- **排序统一用拖动**（行首把手，快捷栏与命令表一致）：拖过一格换一格，松手落盘

**监控** · **文件管理** —— 见 §4.4

**应用内更新**
- 查 CNB Releases 的公开 JSON 接口（发版流水线在 `.cnb.yml`，签名 APK 就挂在那儿的附件上）
- 下载后校验包名 / 版本 / 签名一致再拉起系统安装器

**基础**
- 中英双语，跟随系统，设置内可切
- 暗色主题（终端配色 2–3 套）；字号 / 行距可调

### V2（路线图，不在首版）

参数化快捷命令 · 命令历史 · 自定义附加键位 · 输入手势（粘滞修饰键 / 空格拖光标 / 双指切会话）·
应用锁（生物识别）· 应用内生成密钥 + ssh-copy-id · `ssh_config` 导入 · 加密备份恢复 ·
导入本地字体 · 更多配色

### 明确不做

- **mosh**（GPLv3，见 §3）
- **本地终端 / 内置 shell**（那是 termux 的地盘）
- 字体下载中心、多字体变体打包
- 云同步 / 账号体系
- 桌面端

---

## 6. 技术架构

### 6.1 模块

| 模块 | 职责 | 许可 |
|---|---|---|
| `app` | 产品层：Compose UI、会话编排、SSH 传输、tmux 侧通道 | **MIT** |
| `terminal-emulator` | ANSI 解析 / 终端状态内核（vendored termux + `[lightmux]` 传输改造） | Apache-2.0 |
| `terminal-view` | 终端渲染 View（vendored termux，**未修改**） | Apache-2.0 |

技术栈：Kotlin 2.1 + Jetpack Compose + Material3，minSdk 24 / compileSdk 35，JDK 17，AGP 8.7。

### 6.2 三条主干抽象

**① `TerminalTransport`**（在 `terminal-emulator`，Java）——所有连接方式的统一入口：

```java
void start(int cols, int rows);
void write(byte[] data, int off, int len);
void updateSize(int cols, int rows);
void close();
String exec(String cmd);   // 带外执行，拿 stdout，不占前台 PTY
```

`exec()` 是整个产品的地基：tmux 列表、监控采集、Docker 动作全靠它。SSH 实现复用现有连接开新 channel。

**② `TermSession` + `SessionManager`**——会话常驻在 Application 作用域，**不随导航销毁**。
终端页每次进入重建 `TerminalView` 再 attach 到既有 session（`updateSize` 对已存在 emulator 只 resize 不重建），
滚屏历史与连接得以保留。**这条被打破 = 多会话卖点消失**，改导航时必须守住。

**③ 纯逻辑与 UI 分离**——所有可测逻辑抽成无 Android 依赖的 object：

| 纯逻辑单元 | 职责 |
|---|---|
| `Tmux` | 命令生成 + 输出解析 |
| `HostFacts` | 监控输出分段解析 |
| `SftpPath` / `FilePerms` | 路径规范化、权限互转 |
| `ReconnectPolicy` | 退避状态机 |
| `SshConfig`（V2） | ssh_config 解析 |

不是洁癖：**本机没法编 Compose（可编，但慢且无法真机验证），纯 Kotlin 能直接 `kotlinc` + JUnit 跑**。
分层直接决定了提交前能不能自测。单测放 `app/src/test/`。

### 6.3 导航与返回栈

砍掉底部导航后，**返回键是唯一的层级机制**，它的正确性直接决定 app 好不好用。

- 页面少（主页 / 终端 / 监控 / 文件 / 文件编辑 / 主机编辑 / 设置树），**不引入 navigation-compose**——
  一个 `sealed interface Screen` + 状态机足够，省一个依赖也省一层心智。
- **每新增一个页面，必须同时在 `BackHandler` 补返回路径**。漏了不是「返回不生效」，是**直接退出 app**——
  这是最容易漏、用户最恼火的一类 bug，列为 code review 必查项。
- 监控 / 文件页携带 `fromSessionId`，**从哪来回哪去**：从终端进就退回该终端，从主页的主机行进就退回主页。
- **主页状态必须跨导航保留**：展开了哪些主机、滚到哪、探测缓存——提到 ViewModel 层，
  不放在页面 Composable 的 `remember` 里。否则「进终端 → 返回」会把树折叠回初始态，
  而在没有导航栏的设计里这条路径是最高频的，塌了整个交互就废了。
- 终端页用 `key(sessionId)` 包裹，切会话时强制重建子树，避免 `AndroidView` 残留旧 View。

### 6.4 tmux 侧通道纪律

这几条是真机上验证过的**事实结论**：

1. **一次 exec 完成探测 + 列会话 + 列窗口**，别发三次命令。
2. **`-F` 格式串用 `:` 分隔**——TAB 会被 tmux 替换成 `_`；**名字一律放行尾**，数值字段从行尾反解。
3. **会话身份跨连接用 name**（`$id` 只在单个 tmux server 生命周期内有效）；窗口切换用 `@id`（`会话名:index` 的 index 会移动）。
4. **解析失败返回 `Malformed`，不许伪装成空列表**——空列表会让 UI 显示「没有会话」，用户以为会话丢了。
5. attach 用 `new-session -A -s <name>`（原子，存在则附加），接管加 `-D`。
   **会话上已经有别的客户端时改走分组镜像会话**（`new-session -t <原会话> -s '<原会话> (lightmux)'`
   + `destroy-unattached on`，0.1.57）：组内会话共享窗口集合但各有各的当前窗口与尺寸，
   手机切窗口不再把电脑那块屏一起拽走。**两边落在同一个窗口时仍然互抢尺寸**——
   一个窗口只有一份字符网格，那是 tmux 的物理限制，见文末偏差记录。
6. 动作命令统一包成 `首行 __LM_RC__:<code> + 原始输出（stderr 合入）`，统一解析退出码。
7. **绝不向前台终端注入按键**——前台可能是全屏 TUI，也可能压根没 attach。

---

## 7. 里程碑

| 里程碑 | 内容 | 产出 |
|---|---|---|
| **M0 · 骨架** | 新仓库、Gradle 工程、从 termux 上游取两个模块、许可文件齐备、CI 跑通 | 能编出空壳 APK |
| **M1 · 终端可用** | `TerminalTransport` 改造、SshTransport、TermSession/SessionManager、终端页 + 附加键栏、主机增删改 + 加密凭据 | 能连上服务器敲命令 |
| **M2 · 切换器** | `Tmux` 纯逻辑 + 单测、三级树主页、缓存优先 + 懒探测、attach/新建/重命名/kill/切窗口 | **核心卖点成立** |
| **M3 · 两个入口** | 监控页（`HostFacts` + 单测）、SFTP 文件管理 + 后台传输队列 | 功能面完整 |
| **M4 · 收尾** | 断线重连 + 自动 attach、双语文案、暗色主题、应用内更新、README/截图/开源材料 | **v0.1.0 发版** |

M0→M2 是关键路径，M2 走通即可内部试用。M3 两项相互独立，可分别推进。

> **进度：M0–M4 全部完成**（M4 拆成 M4a 断线重连/保活、M4b 设置与应用内更新、M4c 开源材料与 CI）。
> 唯一未交付项是**截图**——本机没有真机，README 的 Screenshots 一节写明待补。

**验收（v0.1.0）**
1. 冷启动 → 主页显示缓存的会话树 < 500ms，不发起任何网络请求。
2. 展开一台主机 → 3 秒内列出 tmux 会话（局域网）。
3. 从主页到「进入某个 tmux 窗口」**最多 3 次点击**（展开 → 展开会话 → 点窗口），已展开时 1 次。
4. 终端 → 另一个会话 = **2 步**（返回 + 点击），返回后主页保持原展开状态与滚动位置。
5. 切后台 10 分钟回来，会话仍活、滚屏未丢。
6. 拔网重连后自动 attach 回原 tmux 会话，不是新开一个。
7. 依赖树 `./gradlew app:dependencies` 里**零 GPL/LGPL**。
8. 纯逻辑单测覆盖 tmux 解析、监控解析、路径处理、退避策略。

---

## 8. 风险

| 风险 | 影响 | 应对 |
|---|---|---|
| 重写 1 万行 app 层工作量大 | 周期长 | 功能面已收窄（无 mosh / 无字体中心 / 无备份），实际约 8–10k 行；按里程碑切分，M2 即可用 |
| 被认为抄袭同类产品 | 声誉 | 代码全新、包名全新、许可全新；界面按 §4 独立设计——同类产品主页是主机列表，本项目是会话树，形态本就不同 |
| 缓存过期导致点击落空 | 体验 | `new-session -A` 原子语义兜底（§4.3），最坏是新建而非报错 |
| 无 mosh，弱网体验不如带 mosh 的客户端 | 竞争力 | 自动重连 + tmux 自动 attach 覆盖大部分场景——**tmux 本来就是 mosh 的替代品** |
| 从 termux 上游取的模块要自己踩一遍改造的坑 | 返工 | 坑集中在时序、reflow、渲染三处，出问题优先看这几块 |
| 开源后收到 PR 无力维护 | 负担 | `CONTRIBUTING.md` 写清范围与响应节奏；不承诺 SLA |

---

## 9. 仓库与协作约定

- **远端**：`cnb.cool/ninvfeng/lighttools/lightmux.git`（默认推送）+ GitHub 镜像（Issue / PR / Releases 门面）
- **CI**：GitHub Actions 跑 `assembleDebug test` + `scripts/check_strings.py`（push/PR，见
  `.github/workflows/ci.yml`）；CNB `tag_push` 出签名 APK 挂 Release（见 `.cnb.yml`）
- **发布**：tag `vX.Y.Z`，`versionName` = 去 `v`；**每个分发出去的 APK 必须递增 `versionCode`**
- **提交**：Conventional Commits，描述用中文（`feat(tmux): 主页会话树支持展开窗口`）
- **文案**：用户可见文案一律走 `strings.xml`，`values/` 与 `values-zh/` 条目数必须相等
- **注释**：写「为什么」，不写「是什么」
- **不入库**：`local.properties`、keystore、`ARCHITECTURE.md` 等内部文档

---

## 附：待确认

1. ~~GitHub 仓库归属~~ **已定：个人 `ninvfeng/lightmux`**。
   仓库标识只写在 `update/UpdateChecker.kt` 的 `Repo.SLUG` 一处，README 徽章、更新检查 API、
   应用内「项目地址」都由它派生；将来改名或换 owner 只动这一个常量。
2. ~~应用图标与中文名~~ **已定：不取中文名**，两种语言下 `app_name` 都是 `lightmux`。
   图标用自绘的自适应图标（`drawable/ic_launcher_foreground.xml` + `mipmap-anydpi-v26`），
   通知栏图标另有单色版 `drawable/ic_notification.xml`。
3. ~~签名密钥~~ **已定：新生成，独立于任何其他项目**。keystore 不入库，
   由 CNB 密钥仓库经 `LIGHTMUX_KEYSTORE*` 环境变量注入（见 `.cnb.yml`）；
   本地缺变量时 release 构建回退 debug 签名，能出包但无法覆盖升级正式版。

---

## 附：实现与本文的偏差记录

按里程碑落地过程中，以下几处与前文的原始设计不同。**以实现为准**，本节说明原因。

| 位置 | PRD 原文 | 实际实现 | 原因 |
|---|---|---|---|
| §3 字体策略 | 内置 JetBrains Mono Regular（OFL 1.1），APK 约 8MB | 一致；**原样打包 Regular 单字重（268KB）不做子集化**。0.1.1 及之前一度改为不打包字体、用系统等宽，之后改回 | 系统等宽在 Android 上是 Droid Sans Mono：**不含制表符**（U+2500 系列），htop / vim 框线跨字体回退后宽度与格宽不符，被 `TerminalRenderer` 横向拉伸；字面也偏小（x-height 0.53em vs 0.55em），`l`/`i` 在 0.6em 格子里只剩一根光竖线。当初担心的 OFL 保留字体名称（RFN）并不存在——JetBrains Mono 未声明 RFN，何况原样分发不构成衍生品。「导入本地 TTF」仍在 V2 |
| §4.2 导航 | 「终端页侧滑快速切换抽屉」被主动砍掉，主页就是那个抽屉 | 0.1.3 **把抽屉加回来了**，作用于终端 / 监控 / 文件三页；0.1.7 起抽屉里装的**就是 `HomeScreen` 本身**（0.1.3–0.1.6 是另写的只读树） | 反对理由的实质是「同一份会话状态两处渲染会分裂」。共用 `HomeScreen` 之后它彻底不成立：一个 Composable、一份 `HomeViewModel`（Activity 作用域）、一份 `tmuxStates`——抽屉是同一棵树的第二个入口，字面意义上的同一棵。原方案预留的「横向会话胶囊条」替代不了它：那只列会话，不列窗口，而切窗口正是最高频的那条路径。详见 §4.2 的决策修订小节 |
| §4.4 监控 | 一次 `exec` 取全量，采不到标「不可用」；**容器列表挪到后续版本** | 一致；额外落地了**离开页面即停轮询**；0.1.8 **把容器列表加了回来** | 原因是「`docker ps` 依赖用户在 docker 组，多数主机上只会给出一片报错」——这条只在「必须显示点什么」的前提下成立。实际做法是采不到就整段不渲染（同 busybox 的 `ps`），用户一个报错也看不到；而能采到的机器上，「哪个容器挂了」正是开监控页最想知道的一件事 |
| §5 V2 列表 | 「输入手势（粘滞修饰键 / 空格拖光标 / 双指切会话）」整条放 V2 | **粘滞修饰键已在 V1 实现**（`Ctrl`/`Alt` 点一下待发、生效期间高亮），其余仍在 V2 | 附加键栏没有粘滞就等于要求用户双手同时按屏幕上两个键，`Ctrl-B` 打不出来，与「`Ctrl` 属于核心」是同一条理由 |
| §5 V2 列表 / §5 V1 终端 | 「自定义附加键位」「快捷命令」放 V2；附加键栏内容写死 | 0.1.9 **两项都提前落地**：栏上的键可排序、可增删，另有一格「命令」弹出快捷命令表（可增删改）。同时**终端页取消顶栏**，返回变成栏上第一格 | 手机屏就那么高，顶栏那 56dp 换成两行终端更值；但返回一旦没了顶栏就只剩系统手势，而这个 app 的左滑手势已经让给了快速切换抽屉，所以返回必须在栏上有一格——一旦要动这条栏，「顺序写死」就没有再守下去的理由了 |
| §5 V2 列表 | 「端口转发」放 V2 | 0.1.30 **提前落地本地转发（`ssh -L`）**：远端监听端口一键扫、映射到手机本地、浏览器直接开；反向转发（`-R`）仍不做 | 「手机上开着一个跑在服务器 3000 端口的页面」是这个 app 的用户天天要做的事，而它和已有的 `exec` 侧通道共用同一条连接、同一套重连策略，成本只有一层 UI。反向转发不同：它服务的是「把手机上的东西暴露给服务器」，那不是一个 SSH 客户端的日常 |
| §4.2 主机行 | 行尾三个图标入口（终端 / 监控 / 文件） | 0.1.31 **加到四个**，补上「转发」；快捷栏默认键位也补了「文件」「转发」两格 | 0.1.30 把端口转发只放在长按菜单里，理由是「行尾再挤一个就得把主机名截短」——但用户反馈的第一句就是「入口不明显」，说明这个取舍取反了：一个要长按才发现得了的功能，等于没做。图标压到 40dp、彼此贴紧之后共占 160dp，360dp 屏上主机名仍有 144dp，那句担心并不成立。**这 160dp 有个前提**：M3 会给每个可点击组件强制 48dp 的最小触摸目标，`size(40.dp)` 只缩得动画出来的那一格，得在这四格外面把 `LocalMinimumInteractiveComponentSize` 关掉，否则实占 192dp、主机名只剩 112dp |
| §7 CI | 「GitHub Actions 跑 assemble + 单测」 | 另加 `scripts/check_strings.py` 双语文案校验，CNB 发布流水线另加 **tag ↔ versionName 一致性**与**签名不是 debug 回退**两道守卫 | 占位符不一致是运行时崩溃、tag 与版本号错位是废版本，两者都编译不报错 |
| §3 交付物 | `CONTRIBUTING.md` | 已交付，另加 `README.md`（中文，GitHub 首页那份）/ `README.en.md` / `CHANGELOG.md` / `SECURITY.md` | 开源门面 |
| §4.5 转发入口 | 从终端点「转发」跳整页，理由是「配完就去浏览器，不会再回到终端」 | 0.1.35 改成**叠在终端上的半屏面板**（同文件面板）；主页那条路仍是整页 | 顺序想反了：要配转发，正是因为服务刚在这个终端里起来、日志还在滚，而「通没通、端口对不对」的答案就在那几行日志里。整页跳走等于把答案盖住。主页那条路没有这个上下文，整页仍是对的 |
| §5 V1 基础 | 「字号 / 行距可调」 | **只有字号可调，行距砍掉**（理由写在 `data/Settings.kt`） | 行高不是本层能定的：vendored 的 `TerminalRenderer` 直接用字体度量算 `mFontLineSpacing`，要让行距可调就得改 `terminal-view`——而那个模块的价值恰恰在于「和上游一字不差，上游单测因此能原样兜住它」。为一个次要的外观选项破掉这条不划算 |
| §6.1 模块表 | `terminal-view`「仅加行距/字间距可选参数」 | **一行未改**，与上游完全一致（`NOTICE` 与 `grep [lightmux]` 双向可验证） | 同上：行距那一项没做，于是这个模块也就没有任何改动理由。改造全部集中在 `terminal-emulator`，`terminal-view` 保持零 diff |
| §5 功能范围 | 跳板机（ProxyJump）**两个版本的列表里都没有** | 0.1.56 落地：主机表里选另一台当跳板，支持多跳、成环的候选在选择器里就被排掉 | 不是「锦上添花」而是「有没有得用」：内网机器只能从堡垒机进，是国内团队的常态配置，连不上就整台主机用不了，前面所有功能都无从谈起。成本也确实低——sshj 0.40 自带 `newDirectConnection` + `connectVia`，**零新依赖、包体不变**；而 app 里「一条连接开多个 channel」的架构让终端、tmux 侧通道、SFTP、端口转发全部自动跟着走通，改动只落在建连那一段 |
| §4.2 会话去处 | 「底部 Tab 会话列表」被砍掉，活动会话只挂在各自主机节点下 | 0.1.63 在主页树**上方**加一段「已连接」清单，列出全部活动会话（含已 attach 的 tmux 会话），点击回终端、行尾一键断开；树里的裸会话行原样保留 | 当初反对的是**独立成页**——要导航过去、要维护第二份事实来源，而这一段两条都不沾：它就在主页顶部，读的是 `SessionManager` 那条流，和树里的行是同一批 handle，抽屉里也自动跟着有。而树本身回答不了「我现在连着什么」：会话散在各主机节点下，要先展开对应主机才看得见，可想放掉一条连接的用户心里根本没有主机这一层——他要的就是一张还连着的清单。顶栏那个「全部断开」是同一诉求的粗粒度版本，只有全有全无两档 |

| §6.4 侧通道纪律 | attach 就是 `new-session -A -s <name>`，一条命令走天下 | 0.1.57 起是**三段式**：会话上有别的客户端 → 建分组镜像会话再 attach；没有 → 直连原会话（零足迹）；会话不在 → `new-session -A` 兜底 | 手机 attach 一个电脑上正开着的会话，tmux 3.1 起 `window-size` 默认 `latest`，窗口跟着最近活动的客户端来回改尺寸；更实际的问题是手机在主页点一下窗口，电脑那块屏跟着一起切走。分组会话共享窗口集合、各有各的当前窗口与尺寸，两条一起解决。**但同窗口时仍然互抢**：真 pty 实测两个客户端盯着同一个窗口时，无论分不分组都在 `120x38 ↔ 45x18` 之间跟着敲键翻——一个窗口只有一份字符网格，能选的只有「谁赢」，选不了「各看各的」。镜像只在不同窗口时才真正独立。按需触发而不是无脑建：没人跟你抢的时候多一个会话纯属在用户的 `tmux ls` 里添乱 |

单测规模（v0.1.59）：`app` 纯逻辑 **445** 个，`terminal-emulator` 上游原样保留 **138** 个，均须全绿。
