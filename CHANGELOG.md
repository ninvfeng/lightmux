# Changelog

本文件记录 lightmux 的版本变更，格式遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [0.1.62] - 2026-09-06

### Added

- **文件管理能新建空文件了**。顶栏原来的「新建目录」图标改成「新建」菜单，目录和文件各一项——
  再加一个图标会把主机名挤掉，终端页上那个半屏面板更窄。新建走 `open(WRITE|CREAT|EXCL)`，
  `EXCL` 让服务端在同名文件已存在时直接拒绝，不会静默打开一个已有文件让用户以为是新的。
  权限位交给服务端 umask 决定，和上传新文件的处理一致。

## [0.1.61] - 2026-09-06

### Fixed

- **部分服务器上 tmux 窗口切不动，只反复提示「会话列表已过期」**。侧通道的 exec 起的是非交互
  shell，不读 `.bashrc`；探测命令一直补着 `PATH`，动作命令却没补——于是 tmux 装在
  `/usr/local/bin`（FreeBSD、源码编译）或 homebrew 前缀下的机器上，主页照常列出会话和窗口，
  点下去却每次都 `tmux: not found`，server pid 断言不成立而被当成「列表过期」拦下。
  切窗口、开窗口、关窗口、会话改名、断开其他客户端一起恢复。
- **端口转发的监听端口探测在非 root 用户下常常一个都列不出来**。`ss` / `netstat` 装在
  `/usr/sbin`、`/sbin`，非交互 shell 的 PATH 里没有它们，探测不报错、只是返回空。同样补上 PATH。

## [0.1.60] - 2026-09-05

### Added

- **主机表单里能直接跳到密钥管理**。私钥来源那一行右侧新增「管理密钥」入口，点进去导完钥匙按返回，
  表单原样保留、新钥匙已经出现在可选的一排里。此前密钥库空着时这一行整个不显示，
  只剩一个粘贴框的用户根本不知道 app 有密钥管理这回事；现在这一行常驻。

## [0.1.59] - 2026-09-04

### Changed

- **自定义键的「按键」模式改成点选**，不再要求手写 `C-b d` 这套记法。弹窗里是 Ctrl / Alt / Shift
  三个开关（语义同终端里的粘滞修饰键：勾上后下一次加键带上，加完自动清掉）、一排具名键与 F 键的键帽、
  一个只收一个字符的输入框；已选的序列以 chip 展示，点一下移除。
  - tmux 记法退成落盘格式（`format` / `parse` 互逆），管理表里仍按 `C-b d` 这样展示，0.1.58 存的键原样可用。
  - 手写入口撤掉后「认不出的键名」不再可能发生，相应的提示文案一并删除。

## [0.1.58] - 2026-09-04

### Added

- **自定义键支持「按键序列」模式**。此前自定义键只能发一串文本，组合键只有预设的 `^C` `^D` `^B` 那一批；
  现在添加自定义键时可选「按键」，内容按 tmux 记法写、空格分隔：`C-b d`（detach）、`C-b 1`（切窗口）、
  `M-.`（readline 上一个参数）、`F5`、`Up Up Enter`。修饰键 `C-` `M-` `S-` 可叠加，`^b` 等价 `C-b`，
  具名键认 tmux 的 `Enter` `Escape` `DC` `PPage` 与键帽上的 `Ent` `Esc` `Del` `PgUp`，不区分大小写。
  - 有认不出的键名整条不发、弹窗里直接标红，不会「发了半截」把 `d` 单独打进 shell。
  - 序列里每一击的修饰键都是写明的，粘滞 `Ctrl`/`Alt` 不叠加上去。
  - 落盘格式沿用原来的标志位（`0`/`1`/`k`），降级到旧版本时文本键原样可用。

## [0.1.57] - 2026-08-27

### Changed

- **attach 一个电脑上正开着的会话时，改开一个「分组镜像会话」**。tmux 3.1 起 `window-size`
  默认 `latest`，两个客户端连着同一个会话，窗口就跟着最近活动的那个来回改尺寸；
  更实际的问题是**手机在主页点一下窗口，电脑上那块屏跟着一起切走**。
  分组会话（`new-session -t`）共享窗口集合，但**各有各的当前窗口和尺寸**，两条一起解决。

  - **按需触发**：attach 前先在服务端看一眼这个会话上有没有别的客户端，没有就直连原会话，
    **零足迹**——没人跟你抢的时候多一个会话纯属在用户的 `tmux ls` 里添乱。
  - 镜像带 `destroy-unattached on`，手机一 detach 它自己就没了，窗口留在原会话里，
    原会话不受任何影响；镜像也不会出现在主页的会话列表里（原会话被 kill 掉的孤儿镜像除外——
    那时它是那些窗口仅剩的入口）。
  - 侧通道的 `select-window` / `new-window` 跟着打到镜像上。顺带修掉一个目标歧义：
    只给一个 `@id` 而不带会话前缀，在成组时 tmux 会挑「最近活动的那个会话」，切到谁头上全看运气。
  - 会话的「已连接」标记改按**组内客户端数**算，否则手机明明连着，原会话那行却显示「未连接」。

  **仍然做不到的**：两边同时盯着**同一个窗口**时尺寸照样互抢——一个窗口只有一份字符网格，
  能选的只有「谁赢」，选不了「各看各的」。镜像只在两边不在同一个窗口时才真正互不干扰。

## [0.1.56] - 2026-08-25

### Added

- **跳板机（`ssh -J`）**。内网机器要过堡垒机的人此前用不了这个 app——这是功能上的硬缺口，
  不是体验问题。主机编辑页「高级」里选一台跳板机，连接时先连它、再从它开一条隧道钻到目标，
  终端 / tmux 侧通道 / 监控 / SFTP / 端口转发**全都自动跟着走**（它们本来就共用同一条 `SshConnection`）。
  沿用 sshj 自带的 `connectVia`，**零新依赖、APK 不变**。

  几处是刻意这么定的：

  - **跳板机是主机表里已有的一台主机**，不在目标身上另存一份地址与凭据。
    堡垒机本来就是配好的一台机器，复制一份等于同一套凭据存两处，改密码要改两遍，
    漏掉的那一份会在几周后以「连不上」的形式发作。引用之后 known_hosts、密钥库、
    凭据加密全部自动复用，跳板机自己的私钥口令、密钥库引用一样有效。
  - **支持多跳**（跳板的跳板），上限 5 台。链绕成环会让连接线程死循环，所以选择器里
    **直接不列**会成环的主机——环是两台主机配合出来的，报错只能报在后配的那一台身上，
    而用户看着那张表单是想不明白错在哪的。
  - **跳板机被删了就报错，不退化成直连**。目标多半是个内网地址，绕过跳板去撞它，
    用户得到的是一条看不出所以然的超时。
  - **错误分得清是哪一头**：「认证失败」在两头的出路完全不同，所以失败信息带上跳板机名
    （「跳板机 bastion：认证失败」），按钮直达**那台**的编辑页。跳板机的主机密钥变了则照旧
    走原来那个阻断式对话框——它是唯一能让用户信任新指纹的出路，包进跳板错误里等于把它藏掉。
  - **重连决策跟着分**：跳板机连不上算网络问题，照常退避重试；跳板机密码错、链配坏了
    （被删 / 成环）是终态，不拿错凭据反复去撞堡垒机的 fail2ban。
  - 建连中途失败会把已经连起来的那几跳**倒序全关掉**——留一半就是几条挂在堡垒机上的空连接，
    对端 `who` 里看得见，也占着 `MaxSessions`。

## [0.1.55] - 2026-08-24

一轮整体优化。以下每条都是核实过成因的真实问题，不是「顺手整理」。

### Fixed

- **改一台主机的密码，不再抹掉其他所有主机的凭据**。这是本次最严重的一个。
  换锁屏方式 / 恢复出厂 / 换机还原都会让 Keystore 里的密钥作废，此时凭据解不开，
  代码会把它降级成空串并打上「凭据已失效」的标记逼用户重填——但**存回去的时候标记丢了**，
  而且空串被当成新密码重新加密写回。于是只要给任意一台主机重填一次密码，
  整张表被重写，**其余每一台的密文都被空串的密文覆盖**，标记也没了；
  它们此后会拿着空密码去撞服务器，正是 `CredentialLostException` 当初要拦的事
  （既误导用户去查密码，又替对端的 fail2ban 攒计数）。私钥表更糟：passphrase 那一栏直接整个消失。
  现在解不开的字段**原样写回原密文**，标记跟着存盘，往返无损。
- **存储损坏时不再把可恢复的数据毁掉**。解析失败的分支注释写着「绝不覆盖原始数据」，
  可写回那一步是无条件的：JSON 损坏 → 主页空 → 用户新建一台主机 → 原始数据被永久重写。
  现在解析不出来就**不写**。
- **设置读不出来不再让 app 连界面都进不去**。语言要在 Activity 拿到 Resources 之前定下来，
  那一刻抛异常就是启动即崩；而这个 Store 里只有语言 / 主题 / 快捷栏，读失败最多是语言不对。
  改成和主机 / 密钥 / 转发同一套损坏降级。tmux 缓存的读取同理。
- **删深目录不再有把 app 整个带走的可能**。递归深度由远端目录决定，`node_modules` 那种
  嵌套几十上百层一路递归下去就是 `StackOverflowError`——它是 `Error` 不是 `Exception`，
  发生在 IO 协程里没有任何 catch 接得住。改成显式栈，删除顺序与语义不变。
- **端口转发不再出现「界面显示失败、隧道其实在跑」**。失败上报是「先摘表、再写状态」两步，
  中间那一瞬用户重开同一条转发，紧接着的 `Failed` 就把新的 `Starting` 盖掉了；
  更麻烦的是 `Failed` 会让前台服务判定「没活儿」，可能正好在新转发建连的窗口里把自己停掉。
  两步现在是一个原子动作，且只有「自己这一轮」有资格写终态。
- **文件页的时间戳不再可能退回系统语言**。兜底用的是**进程**默认 locale，
  而 app 内的语言开关压根不动它——兜过去等于把用户刚选的语言丢掉。
- **快捷栏划到哪儿就停在哪儿**。横向滚动位置存在 `rememberSaveable` 里，
  而本项目是自研的中央页面栈、没有 `SaveableStateHolder`，每次进终端都弹回开头。
- **主机行右侧四个动作图标的间距对了**。`IconButton` 内部会把可点区域强制撑到 48dp，
  外层写的 40dp 压不过它——四格实占 192dp 而不是注释以为的 160dp，
  360dp 屏上留给主机名的只剩 112dp。现在那一行显式解除强制，写的尺寸才是实际尺寸。
- **转发页和设置页的长文本会截断了**。主机名、备注、进程名、监听地址、更新失败原因
  都是长度不受控的，不截会把行撑成两三行、顶得同行的图标错位。

### Changed

- **APK 从 3.66MB 降到 3.57MB**。三处：release 只打 arm64-v8a / armeabi-v7a
  （x86 那两份 .so 白吃 37KB）；androidx 各 artifact 塞进来的 8 份**一模一样**的
  Apache-2.0 全文合计 25KB，改为在 `assets/licenses/Apache-2.0.txt` 放一份权威副本，
  关于页指过去（和字体 OFL 同样的处理）；排掉只有协程调试 agent 会读的 `DebugProbesKt.bin`。
- **Compose 自带的那套 lint 检查终于真的在跑了**。androidx 新版 lint.jar 用的是新的
  Kotlin Analysis API，AGP 8.7.2 内置的 lint 加载不了，报一句 `ObsoleteLintCustomCheck`
  就**整包跳过**——也就是说 `remember` 误用、组合里起协程、Modifier 顺序、非可观察地读 locale
  这些检查，在这个项目里从来一次都没跑过。用 `android.experimental.lint.version`
  把 lint 单独顶到 AGP 之上即可，不必连 AGP / compileSdk / CI 镜像一起重做。
  开了之后立刻抓到上面那条 locale 问题。
- **exec 侧通道不再每次起一条线程做超时计时**。监控页 5 秒一轮、tmux 探测、每个动作命令
  都走 exec，一分钟十几次线程创建，其中 99.99% 什么也没做就被打断。改用一条共享的定时线程；
  超时真的触发时，关 channel 那一下另起线程做——它是真实 socket 写，
  对端不回确认能卡满 sshj 几十秒的关闭超时，而掉线正是超时最集中的场景。
- 连接池现在会检查借出的连接**还活着**（原先靠的是写在别处的隐式契约，破了的表现是
  「拿到死连接直接抛、且永远不会去拨新的」）；`close()` 改成原子的，并发关只有一条往下走。
- 文件页、主页、转发页收窄了重组范围：传输进度每秒变几十次，原先会带着路径栏、
  整个目录列表和每行的长按菜单一起重算；展开任意一台主机会让屏上所有节点重组。
- 顶栏返回箭头（8 处逐字重复）收进公共组件；密钥页的删除确认框改用已有的通用对话框；
  `LightmuxRoot` 里 12 处一模一样的 ViewModel 样板收成一个辅助函数。净减 59 行。
  同时评估但**没做**的：主页 7 个对话框收成 sealed interface（净减仅 5 行、代价 8 个新类型）、
  形状常量收进主题（全仓只有 5 处、4 个不同值）——为整齐而整齐，不划算。

## [0.1.54] - 2026-08-24

### Fixed

- **设置页滑杆的圆钮不再一高一低**。M3 摆 thumb 和 track 这两个槽位用的是各自的对齐规矩，
  自定义之后圆钮相对轨道偏了。现在圆钮和轨道在同一个槽位里一笔画完，共用一个坐标系，
  圆心正落在轨道中线上。

## [0.1.53] - 2026-08-24

### Changed

- **设置页的两条滑杆换成网页风格的细条**。M3 自带的滑杆是 16dp 粗轨道 + 48dp 触控高，
  两条摞下来正好把「语言」挤出首屏；改成 4dp 轨道 + 14dp 圆钮、整条 22dp 高，
  行的上下留白一并收窄，省出的高度刚好是一条设置项。
- **新建 / 编辑主机的表单改成分组卡片式**。原先八九个带边框的输入框直上直下摞着，
  满屏都是框，「登录后执行」得滚两屏才看得到。现在按「基本 / 认证方式 / 高级」分三张卡片，
  卡片内一行一项——标签在左、输入在右，行间细分隔线；端口与用户名并排占一行；
  私钥 PEM 与登录命令这类长标签、多行输入走「标签在上」的块式，PEM 输入区垫一层浅底。

## [0.1.52] - 2026-08-23

### Changed

- **「全部断开」现在把端口转发也一并停掉**。原先只关会话，转发照旧挂着——而转发比会话更隐蔽，
  它没有页面在前台，断没断全靠通知栏那行字。主页顶栏和通知栏两个入口都改。
  按钮的显隐条件也从「有会话」放宽到「有会话或有转发」：关完终端只留一条隧道是常见状态，
  这时按钮不该消失。确认文案按数量分两句拼，哪边为 0 就不说哪边。
  正在传输的文件不受影响。

## [0.1.51] - 2026-08-23

### Fixed

- **「新增」按钮不再是紫的**。首页、端口转发、密钥三页的 FAB 都没显式给色，而 FAB 默认吃
  `primaryContainer` / `onPrimaryContainer`——主题只定义了 `primary`，这两个槽位于是落到
  M3 baseline 的紫（深色 `#4F378B` 底 + `#EADDFF` 图标，浅色 `#EADDFF` 底 + `#21005D` 图标），
  青绿配色里杵着一颗紫按钮。改用 `primary` + `onPrimary`，两种主题的对比度都比原来高。
  配色收在共用的 `AddFab` 里，三处不再各写一遍。

## [0.1.50] - 2026-08-23

### Changed

- **换应用图标**：从 `>` 提示符 + 光标条改成 tmux 窗格分割。左竖条与底横条构成的
  负空间正好是 lightmux 的 L，右上半透明块是未激活窗格，块之间的 gap 就是 pane border。
  换的理由是原图标没有辨识度——`>_` 是所有终端 app 的通用符号（Termux / Termius /
  JuiceSSH 全一样），在启动器里认不出哪个是 lightmux，也完全没体现 tmux 这个卖点。
  未激活窗格用 alpha 而非写死暗色，好让主题图标（Android 13+ monochrome）重新着色后
  L 不会和它撞成同色。图形主体压在 66dp keyline 内：贴着 72dp 蒙版边界画，
  遇上蒙版略小的启动器就会被削角。
- **通知栏图标同步换成同一个 L**，但去掉半透明窗格块——状态栏只有 24dp，
  那块在这个尺寸会糊成一片灰，反倒把 L 的轮廓吃掉。

## [0.1.49] - 2026-08-22

### Fixed

- **`check_strings.py` 放行字号样例那条文案**。样例是条真能敲的 shell 命令（`$ tmux attach -t 0`），
  中英两份本来就该一样，而「中文与英文完全相同」这条检查把它判成了漏翻，
  0.1.48 的 tag 流水线因此没出包。加进 `SAME_VALUE_OK` 白名单——键帽（`Esc`/`Ctrl`）
  早就在里面，理由是同一条：翻译了反而对不上实体键，命令翻译完则不再是命令。

## [0.1.48] - 2026-08-22

### Changed

- **设置页的终端字号与快捷栏键宽两条滑块加上实时样例**。这两个数原本调完只在终端页看得见效果，
  用户得「设置→终端→嫌小→退回设置」来回试几轮才能定下来。现在滑杆底下各摆一块样例，
  拖的时候当场就定了：字号那条画一行等宽 tmux 命令，并且**跟着终端配色走**——配色选项就在
  上一行，两条一起调时能互相看出效果；键宽那条直接摆真键帽（复用 `QuickKeyCap`）而不是画假方块，
  因为键宽只直接管单字符键，多字符键（`Ctrl`）由文字自己撑开、只跟着缩留白，
  用方块演示会让人以为拖到底所有键都能变那么窄。
- **滑块去掉刻度点**。两条的范围是 21 / 25 档，`steps` 画出来是一排密集小点，既看不清又和样例
  抢注意力；取整从截断改成 `roundToInt`，落在哪一档由标题上的数字和样例本身说了算。
  样例块按最大档（28sp 的字、48dp 的键帽）预留高度，否则从最小拖到最大会把下面整页往下顶。

## [0.1.47] - 2026-08-22

### Changed

- **产品定位重写成「轻量极致、为 tmux 而生的 Android SSH 客户端」**，两句话把卖点摆到最前面：
  整包 **3.6MB**；简约而不简单——除核心 SSH 外还有 SFTP 文件管理、主机概览、端口转发与内置预览，
  以及一条可以按自己习惯重排的快捷栏。原来那句「主页就是 tmux 会话切换器」只说了信息架构，
  没说这个 app 到底有什么、有多大，而这两件事恰恰是选它的理由。改动落到 README（中英）、
  PRD 头部、`CLAUDE.md` 以及应用内关于页的一句话简介。
- **开源许可的文案整体降权**。协议结构（MIT + vendored 两模块 Apache-2.0 + 零 GPL 依赖）
  早就定死也早就交付完了，继续在每份文档里占一整节属于重复陈述。README 的「许可」从
  表格 + 引用块 + 三段说明压成一段；PRD 的「开源合规 review」四个子节合并成一节「许可」；
  `CONTRIBUTING.md` 的「三条硬约束」拆开——许可两条并成「加依赖之前」，文案同步那条独立成节。
  规则一条没删，只是不再摆在需要反复读的位置。
- **`CLAUDE.md` 去掉「法律红线」一节**，其中仍需日常遵守的两条（vendored 模块改动标 `[lightmux]`
  并同步 NOTICE、不引 copyleft 依赖）降级并入「约定」；同时把**包体是卖点**写进约定——
  3.6MB 不是自然形成的，加依赖前先掂量体积得是条明规则才守得住。

## [0.1.46] - 2026-08-22

### Changed

- **全站文案过了一遍，先把标点规则统一**。中文一律不带句末句号（多句之间该断还断），
  英文按「完整句加句号、表单错误与标签不加」区分。原先两种写法在同一屏里混着出现，
  一条对话框正文有句号、下一条没有，看着像哪条没写完。
- **修掉几处「说的和做的不一样」**：设置页的「点一下查 CNB Releases」改成「点一下检查更新」——
  包挂在哪个平台是维护者的事，不该出现在用户眼前；监控页的「首版只支持 Linux」改成「目前」，
  这个 app 早就不是首版了；「读取失败 · 点击重试」补回主语成「读取 tmux 失败」，
  它出现在会话树上，不说清读什么会被当成整页加载失败。
- **术语对齐**：「这把钥匙」统一成「密钥」，与设置页的「密钥管理」一致；
  「进程 Top」改成「进程排行」；通知渠道描述漏了端口转发，补上。
- **设置页分组标题不再只有英文全大写**（`APPEARANCE` → `Appearance`）。
  中文那侧本来就不是大写，两种语言并排看时不该只有一边在喊。
- **端口转发文案里的 unicode 转义还原成明文**。`服务器` 这种写法机器读得懂，
  改文案的人读不懂，等于给后来人埋一次翻译成本。

### Notes

- **README 有几处与实现对不上，改正了**：应用内更新查的是 CNB Releases 不是 GitHub；
  字体是内置的 JetBrains Mono Regular，不是「不内置、用系统等宽」；app 单测数从 205 更到 373；
  路线图里的「自定义附加键位」「端口转发」早就实现了，从「尚未实现」里挪走。
  另外补上了一直没写进去的功能：快捷栏自定义与快捷命令、密钥集中管理、容器监控与占用排序、
  终端里嗅到地址一键转发、后台联网受限提示。
- **文档不再拿 moke 当参照系**。这个项目的代码、产品形态、许可都是独立的，
  用另一个项目解释自己的设计已经没有意义。PRD 的「与 moke 的法律边界」改写成
  「代码来源与许可边界」——红线本身（不拷贝闭源代码、终端内核从 termux 上游取、
  改动标 `[lightmux]`）一条没少，只是不再指名道姓。

## [0.1.45] - 2026-08-22

### Added

- **主机概览里的 IP 可以复制了**。本机 IP 与公网 IP 行尾各有一个复制按钮；
  一台机器有多个地址时按地址分行，各复制各的——拼成一串粘到别处没有任何用处。
- **容器和进程可以按占用倒序**，在 CPU 与内存之间点一下切换，两段各记各的。
  进程那边为此每轮多发一路按内存排序的 `ps`：原先只取 CPU 榜的前几名，
  而真正吃内存的进程往往 CPU 是 0，压根不在那份榜单上，在它里面排内存排出来是错的。

## [0.1.44] - 2026-08-22

### Fixed

- **添加主机时看不到已经导入的密钥**。私钥来源原先是个下拉按钮，按钮上只写着「直接粘贴」，
  没人看得出里面还藏着密钥库的列表——结果是明明导过钥匙，配主机时又粘贴了一遍。
  现在密钥库里的钥匙直接平铺成一排可选项，有哪几把一眼就看得见。

### Changed

- **设置页收紧了留白**，行高、分组标题、滑杆周围都不再空着半行，一屏能多看几条。
- **密钥管理挪到「项目地址」下面**。它原本压在「通用」组的最后一条，
  而想起它的时刻通常是在配主机，翻到底才找得到没有道理。

### Fixed

- **关窗口有可能关掉另一个窗口**。主页记着的窗口编号只在远端那个 tmux 服务活着的期间有效，
  它一旦重启（重启机器、手动 `tmux kill-server`），同样的编号会落到别的窗口头上。
  而列表在探测失败时会继续显示上一次的结果，所以拿着几小时前的编号去关窗口是完全做得到的事。
  现在每次探测会一并记下服务的身份，关窗口和切窗口都先让远端自己核对一遍，对不上就什么都不做，
  提示「列表已过期」并重新读取。**这是本次最要紧的一条修复**——关掉的窗口和里面跑的东西找不回来。
- **停掉的转发有时会把端口一直占着**，再开同一个端口就失败，且失败后只能重启 app 才能恢复。
- **文件下载选完保存位置却什么也没发生**。选择器是另一个界面，它开着的时候 app 界面可能被系统重建，
  重建后就丢了「要下载哪个文件」这件事。在设置里改过语言之后必现。
- **主页主机行上的会话数不刷新**。会话断线、重连时那个数字停在旧值上，要等界面因为别的原因重画才跟上。
- **内置浏览器划回终端后仍在后台跑**。网页里的定时刷新、视频、动画会一直跑下去，白耗电和流量。
- **文件列表的时间戳不跟随 app 内的语言设置**。它读的是系统语言，而且是 app 启动那一刻的，
  在设置里选了语言对它不起作用。
- 传输文件时常驻通知刷新过密，撞上系统限流后反而卡在旧内容上。
- 主机被删之后再进监控、文件、编辑器，提示是一句英文技术信息，现在是正常的中英文文案。
- 英文界面的计数文案不分单复数（"1 sessions"、"1 hosts are using it"）。

### Security

- **加密库升级到最新版**，修掉 1.78.1 上若干已知漏洞。这些漏洞都不在 SSH 用到的代码路径上，
  但一个 SSH 客户端不该停在有已知问题的加密库上。
- **换新手机时不再搬运本 app 的数据**。之前只关掉了云备份，而系统的「设备到设备迁移」是另一条通道，
  默认照搬。搬过去也没用——解密用的钥匙锁在原手机本地的密钥库里，不会跟着走，
  结果是白白多留一份密文在新手机上，用户还要等到每台主机都连不上才发现。现在两条通道都关掉。
- 应用内更新的下载加了大小上限。原来只靠超时兜底，而一条慢速但一直有数据的响应永远触发不了超时，
  可以一路把存储写满。

### Changed

- **界面框架整体升级，视觉风格会有变化**：圆角、动效、配色的默认取值都跟着新版 Material 设计走。
  功能和布局没有改动。
- **安装包 3.2MB → 3.6MB**。上一版刚从 5.3MB 瘦下来，这一版又长回去一些，得说清楚：
  多出来的主要是加密库升级带进来的新算法，以及界面框架两年的更新量，都是压不掉的实体代码。
  同时把加密库新增的几个用不到的模块重新排除掉，找回了 64KB。
- 主题图标：Android 13+ 开启「主题图标」后，桌面上的 lightmux 会跟着壁纸配色走，不再是唯一一个不变的。
- 主页、转发页、监控页的列表在数据变动时重画的范围缩小了，主机和转发条目多的时候更跟手。

## [0.1.42] - 2026-08-22

### Changed

- **安装包从 5.3MB 瘦到 3.2MB**，功能没有任何变化。SSH 客户端本来就该是随手装上去的东西，
  而之前那 5.3MB 里有一多半是加密库带进来、这个 app 一行都跑不到的死重：
  - **1.16MB 是后量子签名算法 Picnic 的常量表**。它是加密库 jar 里的数据文件，
    Android 的两道瘦身工序（资源压缩只看图片文案、代码压缩只看代码）从中间漏了过去，
    所以一直原封不动躺在包里——而 SSH 根本没有能走到 Picnic 的路径。
  - **225KB 是 89 种语言的界面文案**。这个 app 只有中英两套，其余 87 种是系统组件带的；
    存放它们的那张表按格式要求不压缩，有多大占多大。
  - **688KB 是加密库里没人调用的代码**：S/MIME、时间戳、车联网证书这些和 SSH 无关的部分，
    以前因为一条「整个加密库都别动」的保留规则而没法裁掉。

  升级仍是覆盖安装，签名和数据都不受影响。

## [0.1.41] - 2026-08-22

### Added

- **快捷命令也能按条勾「按下即执行」**：和自定义键那个开关是同一个，默认仍是只填不回车
  （误触一条命令跑到线上机器代价太高），但 `docker ps`、`tmux ls` 这种看一眼就完的，
  每次多按一下 ⏎ 也是白按。勾了的那条在列表里带一个 ⏎ 标记——点下去就会跑的命令
  和只填进去的命令，行为差别太大，不能看不出来。
  - 旧格式（0.1.40 之前存的纯命令行）照读，不自动回车：老用户攒下的那几条不该
    因为多了个开关而消失，更不该忽然变成「点一下就跑」。

## [0.1.40] - 2026-08-22

### Added

- **快捷栏可以自己造键**：填一个键帽文字和一串要发送的内容，它就是栏上的一格。
  为的是把常用的那条命令顶成**一级快捷键**——`c1`（`alias claude`）这种一天按几十次的东西，
  藏在命令表里等于每次多两下点击。
  - 「按下即执行」是每个键单独的开关，默认不勾（只填不回车，与快捷命令表一致）：
    误触一条命令跑到线上机器代价太高，但一键就跑正是一级快捷键的意义，所以给开关而不替用户定死。
  - 自定义键和预设键在同一条栏上排序，管理表里能改能删；发送内容显示在键帽旁边，
    几格自定义键排在一起不至于分不清谁是谁。

### Changed

- **快捷栏的「自定义」入口钉在最右侧**，不再跟着键一起横滚。键一多就得划到底才够得着，
  而这一格恰恰是「键太多/太少要调一下」时才去按的——把它藏在划到底的地方，
  等于让想调栏的人先受一次栏的罪。

## [0.1.39] - 2026-08-22

### Added

- **终端上从右往左划，把网页拉出来**：内置浏览器不再是导航栈里的一页，而是终端右边的
  一层「后台页」。网页上从左往右划就把它收回去，**WebView 不销毁**——
  登录态、滚动位置、正在跑的 SPA 原样留着，划回来还是刚才那一屏。
  这个浏览器的用法本来就是「看一眼页面、回终端敲一行、再看一眼」，
  每次返回都重开一遍等于让这条路不可用。
  - 终端上**还没有**后台网页时，同一个手势掀的是转发面板：那是同一个意图的前一半——
    「刚起来的服务，我要看看」，有网页时它就是答案，没有时下一步必然是去配转发。
  - 同一时刻只留一页，再开一个端口就换掉它；操作栏的「×」才是真关掉（WebView 跟着销毁）。
  - 网页盖着时返回键先在网页里后退，退到头把它收回后台而不是关掉。
  - 手势阈值与快速切换抽屉共用一套（半屏起手、48dp 长划、纵向漂移超 40dp 就让开），
    终端底部那条快捷栏照旧不被抢；网页盖着时抽屉手势让位，避免两个手势抢同一次左右划。

## [0.1.38] - 2026-08-21

### Changed

- **转发页/面板一打开就列出远端在监听的端口**，不必再点一下放大镜。点开「转发」这一下
  本身就是「让我看看这台机器上有什么端口」，把结果藏在一个按钮后面只是让人多点一次。
  扫描仍是一条 `ss -tlnp`，只针对当前这台主机。
- **放大镜改成过滤搜索**：筛的是**已经拿回来的**那份列表，不再发起请求。
  一台开发机上二三十个监听端口是常事，滚到底去找 3000 不如打两个数字。
  按端口号、进程名、监听地址匹配（包含即算，忽略大小写），已配好的转发行同时跟着筛。
  - 重新扫描挪到旁边的 ⟳，服务刚在终端里起来时用它刷新。
  - 搜索框开着时返回键先收框再退页面；面板重新展开时过滤词自动清掉，
    不会出现「再打开还筛着、端口像是丢了」。

## [0.1.37] - 2026-08-21

### Changed

- **内置浏览器的操作栏挪到底部并压扁**：栏高从 64dp 降到 44dp，网页从状态栏底下一直铺到栏的上沿，
  中间不再横着一条标题栏——小屏上多出来的是**能看见的内容**，不是留白。
  按键也落到了拇指够得着的地方。
  - 栏中间只显示地址，不再显示网页标题：这个浏览器是给转发端口用的，
    「开着的是哪个口」比「页面叫什么」有用，一行也只放得下一个。点它照样进编辑态。
  - 加载进度条改贴在栏的上沿（2dp），等于给网页画了条下边框。

## [0.1.36] - 2026-08-21

### Added

- **转发出来的端口在 app 内就能打开**：转发页行尾的「打开」、终端那条端口提示条的「打开」，
  现在都进内置的迷你浏览器，不再切去外部浏览器。
  切走的那一下恰恰是省电策略掐掉后台联网的时机——用户看到的是「刚点开就打不开了」，
  正是 0.1.33 那条提示条在解释的事；留在 app 内，前台还是自己，隧道不会当着面断掉。
  - 地址栏可改，**只打端口号也行**：`3000` 就是 `http://127.0.0.1:3000`。
  - 返回键就是后退，网页退到头才关掉这一页；前进和「用外部浏览器打开」收在顶栏「⋮」里。
  - 用系统 WebView，**一个第三方依赖都没加**，安装包只多出这几个类。
  - 只做到「看一眼转发出来的页面」为止：没有标签页、书签、历史库。
  - 同时给环回口开了明文放行（targetSdk 28 起系统默认禁明文，不开的话
    `http://127.0.0.1:3000` 是一个既没有请求也没有报错的空白页）。只放行 127.0.0.1 与
    localhost，流量根本没出过这台设备；其余域名仍必须 HTTPS。

## [0.1.35] - 2026-08-21

### Changed

- **终端里点「转发」不再跳整页**，改成叠在终端上方的半屏面板，和文件面板一个手感
  （往上一拖变全屏，返回键关面板）。要配转发，多半是因为服务刚在这个终端里起来、
  日志还在滚——而「转发通没通、端口对不对」的答案就在那几行日志里，整页跳走等于把答案盖住。
  主页主机行那条路仍是整页：那里没有终端上下文可看。

## [0.1.34] - 2026-08-21

### Added

- **终端里打出地址就提示转发**，像 VS Code 那样。`npm run dev` 刷出
  `➜ Local: http://localhost:5173/` 的那一刻，终端底部浮出一条「输出里有 localhost:5173」，
  点「添加」就建好转发，通了再给「打开」——不必记端口号，也不必先知道 app 有转发这回事。
  - 只认 `localhost`/IPv4/方括号 IPv6，**不认域名**；协议不限 `http`，`redis://127.0.0.1:6379` 一样提。
  - vite 那种 Local / Network 两行只提一条，优先环回口那个。
  - 已经配过的端口不再提；「×」掉的端口本次运行内不再提，重开 app 重新审视。
  - 提示条贴在快捷栏上方而不是弹对话框：服务刚起来多半还要接着看日志，把屏幕挡掉是本末倒置。

## [0.1.33] - 2026-08-21

### Added

- **后台断连有救了**：国产 ROM 会在应用切到后台几秒内掐掉它的网络——
  而端口转发的用法恰恰就是「转完切去浏览器」，隧道当着用户的面就断了。
  前台服务只保得住进程，保不住网络，这件事没有代码层的解法，只能引导用户授权：
  - **转发页顶部多一条提示**，附「去允许」直达系统的电池优化白名单，授权完自动消失。
  - **设置 → 通用 → 后台联网**是长期入口，随时能翻回来看当前状态；
    已允许但仍会断的机型，这里会提示去「电源管理」打开「完全后台行为」。

### Removed

- 清掉从未使用过的 `WAKE_LOCK` 权限声明。唤醒锁挡不住后台联网限制，
  留着只是让安装时的权限列表多一行。

## [0.1.32] - 2026-08-21

### Fixed

- **端口转发根本连不上**（0.1.30 起，即端口转发发布至今一直是坏的）。
  本地端口绑到了 IPv6 环回口 `[::1]` 上，而「在浏览器中打开」给的是 `http://127.0.0.1:<端口>`，
  两边错开一个协议栈，浏览器只会看到「拒绝连接」。
  - 起因是 `InetAddress.getLoopbackAddress()` 在 Android 上返回的是 `::1`——
    libcore 里写死 `Inet6Address.LOOPBACK`，和桌面 JVM 的行为不一样。
  - 这个 bug 特别难查：端口**绑得上**，所以不报错、状态照样显示「转发中」、
    「在浏览器中打开」也照给，看起来一切正常，只有浏览器那一下是失败的。
  - 现在绑定地址和浏览器地址共用同一个常量，不可能再各走各的。

## [0.1.31] - 2026-08-21

### Changed

- **端口转发不用再长按找了**，两处直给入口：
  - **主页主机行尾多一个图标**，和终端 / 监控 / 文件排在一起，一眼看得见。
    四个图标压紧了排，主机名的显示宽度没有变短。
  - **终端页快捷栏新增「转发」一格**，紧挨着「文件」。终端里刚 `npm run dev` 起来的那个服务，
    不用退回主页——就在这儿转出来，然后去浏览器里开。
  - 老用户如果自己排过快捷栏，栏上不会自动多出这一格（不能动用户排好的顺序）；
    要的话去「设置 → 快捷栏」把它加上。
- 快捷栏的默认键位补齐成「返回 · 命令 · 文件 · 转发」四格动作键，然后才是真按键。

## [0.1.30] - 2026-08-21

### Added

- **端口转发**（主页长按主机 → 端口转发）。服务器上跑在 3000 端口的东西，
  手机浏览器里直接开 `http://127.0.0.1:3000`——等价于 `ssh -L`，不用装 frp、不用开公网端口。
  - **一键扫远端在监听什么**：`ss -tlnp` 列出来，只绑 `127.0.0.1` 的排在最前面。
    那些正是非转发不可的——绑 `0.0.0.0` 的本来就能按 IP 直接连。点一下「转发」就配好并开始转。
    和主页的懒探测同一个规矩：**点了才扫**，不进页面就先拨一条连接上去。
  - **只绑环回口**，转发出来的端口只有这台手机连得上。绑 `0.0.0.0` 意味着连一次咖啡店热点，
    就把服务器上那个「只监听 127.0.0.1」的数据库开给了整个网段，而用户完全不会意识到。
  - 本地端口默认和远端同号（远端 3000 → 本地 3000），最省心智；远端是 80 / 443 / 22 这类
    特权口时落到 8080 / 8443 / 8022——Android 上非 root 应用绑不了 1024 以下的端口。
  - 转发通了行尾会出现一个「在浏览器中打开」；没通就不给这个按钮，
    否则点开只会看到一个连接失败页，用户会以为是服务端的问题。
  - **断线自己重连**（退避 + 网络恢复立即重试），活着的转发也算进前台服务的存活条件——
    手机切后台进程被回收，隧道跟着断，而用户此时正盯着浏览器里那个页面。
  - 「目标地址」默认 `localhost`，填成内网别的机器就把这台主机当跳板用。
  - 转发和 tmux 列表、监控、SFTP 共用同一条 SSH 连接的机制，不额外多拨一条。

## [0.1.29] - 2026-08-20

### Changed

- **不再申请通知权限，通知栏那条常驻通知默认不出现**。它从来不是给用户看的东西，
  只是「前台服务」这个保活机制的强制附属品——Android 要求进程想活着就得挂一条通知。
  但 Android 13 起通知是运行时权限，**没授权系统就不显示它，服务照跑、保活能力一点不少**，
  所以那个权限弹窗对用户是纯负担：给了多一条常驻通知，不给什么也没损失。
  - 会话保活没有任何变化：切后台、锁屏，SSH 连接与滚屏历史照旧。
  - 通知里那个「全部断开」也不是唯一入口，主页上本来就有。
  - 想留着这条通知的，去系统设置里给 lightmux 打开通知即可（权限声明仍在）。
  - Android 12 及以下没有这个运行时权限，通知仍会显示——系统层面绕不过去。

## [0.1.28] - 2026-08-20

### Changed

- **终端里的文件管理改成半屏面板**，和快捷命令表同一套手感：从下往上滑出半屏，要翻目录
  再往上一拖变全屏。原先是整页跳走，而从终端点开文件十有八九是为了「挑个路径填进正在敲的
  那行命令」——命令上下文（敲了一半的那半行、上一条的输出）被整页盖住，回来还得重新对照。
  - 「输入到终端」点完面板自己收起来，终端就在下面，路径已经填在光标处。
  - 面板里的**返回键 = 关面板**（下弹窗的通用预期），上一级走路径栏左边那个 `↑`。
    目录状态留在原处，关掉再点开还停在刚才那个目录。
  - 主页进来的文件管理**仍是整页**：那儿背后没有需要看着的东西，半屏只是白白变小。

### Changed

- **文件页的路径栏压成一行**。原先是一排面包屑按钮，一级一个，`/var/lib/docker/volumes`
  光按钮留白就吃掉大半屏；顶栏还另外重复显示一份同样的路径。现在按路径本来的样子连着写一行，
  顶栏那份撤掉，只留主机名。
  - 左边是**上一级**图标。它走的是「到上层目录」而不是返回键那套「退回来时的路」——
    在浏览起点按返回键是关页面，但这个图标按下去该永远有反应。
  - 右边是**复制路径**和**输入到终端**，针对的是当前目录。和长按某一行拿到的不是一回事：
    `cd`、`tar -C` 要的是目录，不是目录里的某个文件。
  - 多级跳转没丢：**点路径本身**弹出祖先列表，一下跳到 `/var`，不用一级一级往上点。
  - 路径太深时自动滑到末尾——「我在哪」写在最右边，横滚条停在开头等于只给看 `/var/lib/do…`。

## [0.1.26] - 2026-08-20

### Added

- **快捷栏多了一格「文件」**，在终端里一点就进这台机器的文件管理，不用退回主页再挑主机。
  从终端进来的文件页认得「回哪个终端」，所以长按任意文件或目录，菜单里除了复制路径，
  还多一条**「输入到终端」**：点下去就回到刚才那个终端，路径已经填在光标处。
  在手机上照着屏幕手打 `/var/lib/docker/volumes/...` 才是真正劝退的那件事。
  - 填进去的路径**一律先过 shell 引用**：带空格的文件名会被拆成两个参数，
    而一个叫 `$(...)` 的文件原样填进去是会**被执行**的。
  - 只填不回车，与快捷命令表的规矩一致——误触一条命令跑到线上机器的代价，比多按一次 ⏎ 高得多。
  - 同一台主机的文件页状态是留着的，第二次进来停在上次那个目录，不会每次弹回家目录。
  - 已经调过快捷栏顺序的用户，这一格要去栏尾的「管理」里加：默认顺序只对没动过的人生效，
    否则每次加键都会把用户排好的顺序推翻。

## [0.1.25] - 2026-08-20

### Changed

- **安装包从 15 MB 瘦到 5.2 MB**。R8 的 keep 规则其实一直写在 `proguard-rules.pro` 里，
  只是 `isMinifyEnabled` 从来没打开过——省下来的大头是 material-icons-extended 里几千个
  从没用到的图标类。sshj / BouncyCastle / eddsa 仍然整包 keep 不动：它们靠字符串反射装载算法实现，
  精细化裁剪省下的那点体积，换来的是「某台机器的密钥算法突然不支持」这种只在真机上才现形的故障。
  另外补了一条 `-keepnames class * extends java.lang.Throwable`——异常类名是给用户看的兜底文案
  （`e.message ?: e.javaClass.simpleName`，代码里有七八处），混淆后连不上时会显示成一个单字母。
- **在文件页拉开快速切换抽屉，现在能看到真实的会话列表**。抽屉的静默刷新只肯蹭现成的连接，
  蹭不到就显示带时间戳的旧缓存（为渲染一个抽屉去握手认证，等于把「绝不自动全量探测」作废）。
  但文件页那条 SFTP 连接一直不在侧通道的视野里，于是人明明连着这台机器，抽屉却只给一份陈快照。
  现在侧通道能蹭它了，仍然不拿所有权——关它的还是文件页自己。

### Fixed

- **网络恢复时的抢跑重连不再可能重复建连**。`registerDefaultNetworkCallback` 的回调跑在
  ConnectivityManager 自己的线程上，而那条路径直接改了退避状态和重连 Job——这两样按设计只该被主线程碰
  （同一个类里的 `onConnected` 就老实切回来了，这里是漏网）。Wi-Fi 切蜂窝恰好撞上退避倒计时的话，
  主线程那次 `cancel()` 可能看不见另一条线程刚写进去的 Job，剩一个孤儿倒计时把同一个 tmux 会话
  attach 第二次——两个客户端会按最小的那个协商窗口尺寸，屏幕于是莫名其妙被压小。
- **侧通道的「按主机串行」不再会失效**。发锁用的是 `getOrPut`，而它在 `ConcurrentHashMap` 上是
  读改写两步：展开主机的同时监控页正好开轮询，两边各拿到一把不同的锁，于是同时拨两条连接到同一台机器。
  白花一次握手认证事小，短时间反复连同一台机器容易踩 fail2ban 事大。SFTP 那把「建 / 拆连接」的锁
  同时从全局改成按主机——建连要等满 20 秒超时，一台连不上的机器不该让其他机器的浏览和传输一起排队。
- **多选上传不再卡一下界面**。取文件名和大小走的是 SAF 的跨进程查询，第三方 provider 上一次几百毫秒，
  一口气选十几个文件就是十几次串行 binder 全压在主线程。现在查询挪到 IO，
  入队顺序仍然是选择器里挑的那个顺序。

### Added

- **双语文案的自动护栏**。「`values/` 与 `values-zh/` 条目数必须相等」一直是靠人肉数的约定；
  漏翻一条，中文用户就在满屏中文里看到一句英文。占位符对不上更糟——`getString(id, arg)`
  会在运行期抛 `IllegalFormatException`，而这种崩溃只在那条分支被走到时才现形，多半是用户先撞上。
  现在三条纯 JVM 测试兜着：键集合一一对应、同一文件里没有重名、占位符逐条比对（排序后比，
  中英语序不同是正常的，要管的是有没有少一个、`%1$d` 有没有被译成 `%1$s`）。

## [0.1.24] - 2026-08-19

### Changed

- **排序改成拖动，行首加把手**（快捷栏与快捷命令表统一）。上一版给命令表加的那对上下箭头
  只解决了「能不能排」，没解决「排得动不动」——把末尾那条挪到第一位要点十几下，
  而且四个图标挤在一行，命令文本只剩半行宽。现在按住把手拖，拖过一格换一格，
  松手才落盘（拖动中每格都写一次 DataStore 的话，列表要等回灌才更新，手指底下那行会一格一格
  地追着走）。命令行随之瘦回三件套：把手 · 命令 · 改 / 删，文本区宽了一截；
  点击填入终端的热区也收进文本本身——把手和按钮各管各的，想拖一下结果把命令填进终端太吓人。
  列表短，没做拖到边缘自动滚屏。

## [0.1.23] - 2026-08-19

### Added

- **快捷命令能调顺序了**（命令表每行加了上移 / 下移，和快捷栏那对箭头一致）。
  此前新命令一律追加在末尾，想把常敲的那条挪到手边只能删了重加，而重加又落到末尾——
  等于为了挪一条把整张表重排一遍。命令表本来就是「常敲的那几条」，顺序即优先级。

## [0.1.22] - 2026-08-19

### Added

- **首页顶栏加了「全部断开」**（刷新左边，有活动会话时才出现）。此前想一次断掉所有连接，
  只能去下拉通知栏点那条前台服务通知——app 自己反而没有入口。行为和通知栏那个完全一致：
  只关本地会话，**不碰传输队列**（正传着的大文件不该被顺手掐了），也不动侧通道连接；
  服务器上的 tmux 会话照常跑着，回来再 attach 就是。挨着刷新，误触代价太大，二次确认。
  没有会话时整个图标不显示，而且它插在刷新左边，出现与消失都不会挪动刷新和设置的位置。

## [0.1.21] - 2026-08-18

### Changed

- **关闭挪到行尾图标上，不再藏在长按里**。上一版给窗口加了关闭，但入口是长按——
  会话的 kill 也一样。长按在这棵树上本来就是「重命名 / detach」这种低频项的去处，
  把「关掉它」塞进去，等于加了个功能又让人找不到。现在裸会话、tmux 会话、窗口三级的
  `✕` 在同一列对齐，扫一眼就知道每行怎么关；会话行的 `✕` 和展开箭头并排，误触由
  原有的二次确认框兜住。长按菜单只留重命名与 detach。

## [0.1.20] - 2026-08-18

### Added

- **窗口可以关掉了**（窗口行长按 → 关闭窗口，二次确认）。此前这棵树上只有会话能 kill，
  窗口只进不出——一台机器开着七八个窗口，想收拾一下只能进终端敲 `exit`，而那正是这个产品
  想省掉的一步。走侧通道 `kill-window -t @id`：用 `@id` 不用 index，index 会随着窗口增删
  往前挪，拿它去 kill 就是在关隔壁那个，而这个动作不可逆。关掉最后一个窗口会连会话一起结束，
  那是 tmux 的语义，不拦——tmux 里本来就没有空会话，拦了等于替用户决定他想留一个。
  确认框把这句话说明白。

## [0.1.19] - 2026-08-18

### Added

- **没装 tmux 的主机可以一键装上**。此前展开这样一台机器只得到一句「这台机器没装 tmux」，
  接下来要么自己开终端敲包管理器命令，要么它在主页上就是个死角。现在探测顺带认出包管理器
  （apt-get / dnf / yum / pacman / apk / zypper / brew / pkg）和当前 uid，那一行变成「点击安装」，
  **确认框里原样摆着将要执行的命令**——这是代用户改动他的系统，批准之前得看得见批准的是什么。
  确认后在新终端里跑，不走侧通道：sudo 多半要密码、包管理器会弹询问、`apt-get update`
  在慢机器上远超侧通道的 8 秒超时，而且这条命令的每一行输出都该让人看见。认不出包管理器时
  仍旧只是一句结论，不摆一个点了没反应的按钮。装了 tmux 的机器（绝大多数）一条 `command -v` 都不多跑。
- **新建 tmux 会话**。主机展开后末尾多一行「新建 tmux 会话」，填个名字就进去，默认名避开已有的会话。
  用的还是 `new-session -A`，和 attach 同一条命令——于是「新建时撞上同名会话」这个分支根本不存在。
- **新建窗口**。会话展开后窗口列表末尾多一行，建完直接落到新窗口里，而不是建完停在主页让人再点一下。
  已经 attach 着的走侧通道 `new-window`（前台可能正跑着全屏 TUI，绝不注入按键）；没 attach 的
  把建窗口和 attach 合成一条登录命令，省一次往返。

### Changed

- **原来的「新建会话」改叫「新建终端」并排到 tmux 那行下面**。它开的是裸 SSH 连接，断了就没，
  而这个产品的全部价值在于会话活得比连接久——把 tmux 藏在第二行是说反了主次。

## [0.1.18] - 2026-08-18

### Added

- **概览页的网络段多了「本地 IP」和「公网 IP」两行**。此前这一段只有速率，
  想知道这台机器的地址还得回终端敲 `ip addr`。公网 IP 由远端主机向外请求一次拿到——
  网卡上挂的多半是 NAT 后的内网地址，这是唯一的办法；它几乎不变，所以只在进页面时采一次，
  不跟着 5 秒一轮的采集反复请求。出不去网的机器如实显示「不可用」。
- **容器多显示 CPU 与内存占用**（`docker stats --no-stream`，podman 同）。
  停掉的容器不在 stats 输出里，那几个字段就留空——不补 0，否则会让人以为它还在跑。
  这一步排在容器列表之后并单独带 6 秒 timeout：它挂了至少还剩一份列表。

### Changed

- **每核 CPU 默认收起，点 CPU 那条进度条展开**。几十核的机器一进页面就是一屏进度条，
  把内存、磁盘、容器全推到了屏幕外。单核机器不给这个开关——展开了也只有一条和总览重复的条。
- **虚拟网卡默认收起**，行尾显示「N 个虚拟网卡」，点开才列。一台跑容器的机器上光 veth
  就能刷满一屏，而真正对外的那张卡被埋在里面。`br0` 这类裸网桥**不算**虚拟网卡——
  KVM 宿主机上它就是唯一的对外通路，收起来等于把真实流量藏了；docker 建的网桥叫
  `br-<hash>`，认得出来。
- 物理网卡行会带上自己的 IP，多网卡的机器上才看得出哪张卡是哪个地址。

## [0.1.17] - 2026-08-16

### Added

- **私钥可以从本地文件选**。此前只有一个粘贴框，而私钥基本都是躺在下载目录或网盘里的文件，
  在手机上选中几十行 base64 复制过来经常掉字符——掉了也看不出来，只能得到一句「认证失败」。
  现在主机表单和密钥管理页都有「从文件选择」。
- **密钥管理（设置 › 密钥管理）**：导入一次，多台主机共用同一把钥匙。主机只记住这把钥匙的
  id，不再各存一份 PEM——换钥匙时改一处，所有引用它的主机跟着变，而不是挨个编辑主机。
  删除前会告诉你有几台主机在用它。
- 导入时就认格式：PuTTY 的 `.ppk` 会当场提示先用 `puttygen` 转换，而不是等到连接时报认证失败。

## [0.1.16] - 2026-08-16

### Removed

- **新建主机的表单砍掉「分组」和「跳板机」**。分组填完哪儿也不显示——主页那棵树是按
  主机 / 会话 / 窗口三级展开的，从没有按分组归拢过任何一层，这个输入框存在的唯一效果
  是让新建主机多滚一屏。跳板机同理，它是照着桌面 SSH 客户端抄来的：为一个单层 ProxyJump
  留着下拉框、自引用校验、连接层那条隧道和一整条「跳板机不存在了」的错误分支，
  代价比它在手机上的用处大。
- **已经配了跳板机的主机，升级后会直连目标地址**。原先只有经跳板才到得了的机器会连不上，
  请改填一个直连可达的地址。

## [0.1.15] - 2026-08-15

### Changed

- **「检查更新」现在真的告诉你新版改了什么**。此前弹窗里那段话是写死在流水线里的模板，
  每个版本一字不差——用户唯一能看到变更说明的地方，写的却是「变更详情见 CHANGELOG.md」，
  而那是个只能点「下载并安装」的手机对话框。现在 Release 描述由 CHANGELOG 对应版本的段落生成，
  发版时抽不到就直接让流水线红，不再发出说明为空的版本。
- 弹窗里的说明按手机排版：接回 CHANGELOG 为 80 列宽屏幕手工折的行，擦掉 `**` 和反引号，
  内容超过一屏时自己滚——不滚的话「下载并安装」会被长说明顶出屏幕，只剩取消能点。

## [0.1.14] - 2026-08-15

### Added

- **快捷栏加了组合键**：`^C` `^D` `^Z` `^L` `^R` `^A` `^E` `^U` `^K` `^W` `^X` `^B`，
  去末尾的「管理」里加进来即可。粘滞 `Ctrl` 要按两下、还得在软键盘上瞄准那个字母，
  而 `^C` `^D` 恰恰是最急的两个——程序跑飞了的时候没空瞄。`^B` 是 tmux 默认前缀键，
  这个 app 的主场就在那儿。键帽用终端里通行的 `^X` 写法，比「Ctrl+C」短得多。

### Changed

- **快捷栏键位大小改成滑块**，24–48dp 连续可调，不再是紧凑 / 标准 / 宽松三档。
  手指粗细因人而异，三档拍板总有人嫌大或嫌小，而这个数本来就是连续的。
  拖的是键宽，**键高与键距跟着一起变**——只把键帽撑大而键仍然挨着，误触邻键的概率一点没降。
  0.1.11–0.1.13 里调过标准 / 宽松的，升级后按原来的宽度接着用，不会被打回紧凑。

## [0.1.13] - 2026-08-15

### Fixed

- **快捷栏的键距终于真的能调了**。Material3 给每个可点击组件强制 48dp 的最小触摸目标：
  键帽画成 26dp，**布局上仍旧占 48dp**，多出来的 22dp 就是你看到的那片空档——
  0.1.10 到 0.1.12 调的键帽、键距、留白全被这条隐形下限吃掉了大半。
  真机实测（1240px 宽、密度 3.5）：每个键的点击区 168px = 48dp，而键与键的真实空隙只有 4px = 1dp，
  也就是「间距」里 95% 根本不是间距，是撑大的键本身。
  现在这条栏自己说了算，紧凑档一屏放 13 个键；管理表和别处的按钮仍守 48dp。

## [0.1.12] - 2026-08-15

### Changed

- **紧凑档再紧一轮，一屏从 11 个键变 13 个**：键帽 30×30 → 26×28，键距 2dp → 1dp。
- **键面留白跟着档位一起缩**（4dp → 紧凑 2dp / 标准 5dp / 宽松 8dp）。
  这一项才是多字符键真正的宽度来源：`Ctrl` `Home` `PgDn` 这些键宽由文字自己撑开，
  只调键帽下限对它们一格都省不出来。
- **三档的键距差拉到 3dp 以上**（1 / 4 / 8）。原先是 1 / 2 / 4，换档几乎看不出区别——
  而宽松档如果只把键帽撑大、键仍然挨着，误触邻键的概率一点没降，那档位就白给了。
  单测里加了一条守着这个差值。
- 图标跟着键帽缩（14–20dp），否则紧凑档的 26dp 键面上会挤成一个几乎顶到边的方块。

## [0.1.11] - 2026-08-15

### Added

- **快捷栏键位大小可调**（设置 → 外观）：紧凑 / 标准 / 宽松三档，
  360dp 宽的机器上一屏分别露 11 / 9 / 7 个键。**默认紧凑**——排在方向键之后的 `Ctrl`
  落在第 9 格，只有紧凑档不用横划就够得着。手指粗细因人而异，所以给档位而不是替所有人拍板。

### Changed

- **设置页把版本和检查更新并成一行**：装的是哪个版本、有没有新版，本来就是同一个问题的两半，
  拆成两行等于让用户自己对照着看。整行点击即检查更新，关于页退成行尾的 ⓘ 图标
  （Apache-2.0 要求的 NOTICE 在那一页，入口低频但不能没有）。

## [0.1.10] - 2026-08-15

### Changed

- **快捷栏排得更紧**：键帽最小尺寸 44×40 → 36×32，间距与内边距 6dp → 3dp。
  一屏能露几个键直接决定 `Ctrl` 要不要划一下才够得着——360dp 宽的机器上从 7 个变成 9 个，
  默认顺序里的 `Ctrl` 正好进到不用划的范围内。再往下就按不准了，30dp 是不再考虑的底线。
- **回车键帽改画 `Ent`**，不再用 `⏎`：符号在缩小后的键帽上糊成一团，三个字母反而认得快。

### Added

- **快捷命令**：快捷栏第二格「命令」弹出一张表，点一条就填进终端。可增、可删、可改，
  自带 `ls -la` / `cd ..` / `df -h` / `free -h` / `docker ps` / `tmux ls` 六条。
  **只填不替你回车**——误触一条命令跑到线上机器的代价，比多按一次 `⏎` 高得多。
- **快捷栏可自定义**：末尾一格进管理，键位上移 / 下移 / 移除，另有 26 个可选键
  （多出 `Home` `End` `PgUp` `PgDn` `Del` 与 `\` `*` `$` `:` `_` `'` 一批符号）随时加回来，
  一键恢复默认。顺序存的是键的 id，装到没有某个键的旧版本上只丢那一格，不会把整条栏打回默认。

### Changed

- **终端页取消顶栏**，整个上半屏留给终端；返回挪到快捷栏第一格。
  手机屏就那么高，标题栏那 56dp 换成两行终端更值；会话标题在快速切换抽屉里看得到。
- 快捷栏默认顺序改为 返回 · 命令 · `⏎` `Esc` `↑↓←→` `Ctrl` `Tab` `Alt` `-` `|` `~` `/`。

### Fixed

- **在快捷栏上从左往右滑不再拽出侧滑抽屉**，划它就是横滚它自己。
  抽屉的起手判定挂在祖先节点的 Initial pass 上，子节点消费不掉，改由快捷栏反过来报出自己的位置，
  落在这块区域的横划抽屉一律让开。
- 快捷栏补上导航栏内边距：`Scaffold` 不管 `bottomBar` 的 insets，原先最底下一排键压在导航栏里。

## [0.1.8] - 2026-08-15

### Added

- **监控页加上容器列表**：名称、镜像、状态（`Up 3 days` / `Exited (0) 2 hours ago`），
  运行中的排在最前面并高亮，停掉的按名字排在后面。状态保留 docker 的英文原文，
  和你在终端里 `docker ps` 看到的对得上。
  没装 docker 就试 podman（RHEL 系默认只有它）；两个都没有、或当前用户不在 docker 组，
  **整段不显示**——不摆报错，也不摆一个空标题。不采 CPU / 内存：那要 `docker stats`，
  即便 `--no-stream` 也得停一秒，而它和别的指标共用同一条 exec。
  命令套了 `timeout`：dockerd 卡死时 `docker ps` 会一直吊在 socket 上，
  不拦着它能把整页监控一起拖到超时。

## [0.1.7] - 2026-08-15

### Changed

- **侧滑抽屉里装的就是主页本身**，不再是另写的一棵只读树。抽屉从此和主页完全同功能：
  长按重命名 / 断开其他客户端 / 结束会话、新建会话、裸会话、下拉刷新、顶栏的刷新与设置入口，
  一样不少——原先这些在抽屉里全都没有，因为那是另一套行组件。
  改法很直白：抽屉的内容就是 `HomeScreen`，与主页共用同一个 `HomeViewModel` 实例。
  两处渲染同一份会话状态，最怕的是「加载中 / 读取失败 / 没装 tmux / 没有会话」这四个分支
  哪天在其中一处漏掉一个，而那个 bug 长得就像「用户的会话没了」；一份实现就没这个漏。
  抽屉里展开哪台主机也不再单独记一份，和主页同步。

## [0.1.6] - 2026-08-14

### Changed

- **设置页把版本、检查更新、项目地址三行并到最上面**，原来的「关于」分区就此取消，不留空壳

### Fixed

- **抽屉从左往右滑完全划不出来**（0.1.4 引入）。上一版为了治「上下滑时侧边冒出来一点」，
  既禁掉了抽屉自带的拖拽、又只认最左边 24dp 起手，两者一叠加就没剩下可用的路径了——
  Android 10+ 的手势导航把两侧边缘的右滑当「返回」抢走，而 `systemGestureExclusion`
  每条边最多只排得掉 200dp 高，那条带子之外全归系统。现在起手区放宽到屏幕左半边，
  用户自然会从离边缘远一点的地方起手；「是不是要开抽屉」仍然只由位移阈值判
  （右划满 48dp、纵向漂移不超过 40dp、且横向明显占优），所以 0.1.4 治好的误开不会回来。
  顺带修掉一个隐患：手势的 `pointerInput` 原先拿一个每次重组都新建的 lambda 当 key，
  每重组一次就把手势协程重启一次，划到一半的手势会被就地取消

- **重连会把电脑上的 tmux 客户端踢下线**。重连时发的是带 `-D` 的接管式 attach，
  意思是「把这个会话上别的客户端全踢掉」——手机进一次电梯，桌面的 tmux 就 detach 一次。
  当初加 `-D` 是怕服务端那个还没超时的僵尸客户端把窗口尺寸压小，但 tmux 3.1 起
  `window-size` 默认 `latest`，尺寸跟最近活动的客户端走，这个担心早就不成立了。
  现在重连走普通 attach；**踢别人仍然可以，但必须是用户自己在主页点「断开其他客户端」**
- **断线其实从来没有自动重连过**，一直是弹「连接已断开」等用户手点。
  读线程的异常本来就是吞掉的，等 channel 关闭的那一步又裹了一层 `runCatching`，
  于是「网线断了」和「用户敲了 exit」在上层长得一模一样——后者本就不该重连，
  判断逻辑便把两者一起判成终态，整套指数退避对最常见的掉线形同虚设。
  现在连接异常会如实往上抛，网络抖动 1s 后自己重连回来，用户不必再点那一下
  （也就不会再触发上面那条 detach）

## [0.1.5] - 2026-08-14

### Fixed

- **应用内更新装不上：系统提示「已安装」，版本却纹丝没动**。原先把下载好的 APK 包成
  content URI 用 `ACTION_VIEW` 甩出去，等于「谁能处理 apk 谁来装」——接手的未必是系统安装器
  （不少 ROM 会截给自家的安装/安全中心），而且**发完 Intent 就撒手，安装结果一个字都回不来**，
  应用只能傻等着转回空闲态。改走 `PackageInstaller` 会话：字节由应用自己写进会话再提交，
  装的必定是校验过的那个文件；结果经广播回到应用，失败时把系统的原话直接摆在设置页上，
  不再是「什么也没发生」。
  下载、三重校验（包名 / 版本严格更高 / 签名一致）一字未改——这次的问题从来不在那一段。

> ⚠️ 这个修复在**下一次**更新时才生效：0.1.4 及更早版本装新包用的仍是旧路径。
> 卡在旧版本上的话，去 [Releases](https://cnb.cool/ninvfeng/lighttools/lightmux/-/releases)
> 手动装一次 0.1.5，之后的应用内更新就走新路径了。

## [0.1.4] - 2026-08-14

### Added

- **附加键栏最前面加了回车键 `⏎`**：软键盘上回车在最右下角，单手握持时最够不着，
  而它偏偏是最高频的一个键

### Changed

- **设置页把「检查更新」提到最上面**：这一页只有它是「有事才来」的入口，
  主题、语言这些设一次就不再动
- **抽屉的边缘手势阈值调高**：原先只要横向位移过了 touchSlop（~8dp）就开抽屉，
  上下滑列表 / 滚终端时手指带一点斜就够得着，抽屉从左边探出来一截又缩回去。
  现在要往右划满 56dp、纵向漂移不超过 40dp、且横向明显占优才开；
  抽屉关着时还一并禁掉了 `ModalNavigationDrawer` 自带的跟手拖拽——
  它的起手判定只看横向位移、完全不比较纵向，才是「侧边自己冒出来一点」的真正来源

## [0.1.3] - 2026-08-14

### Added

- **快速切换抽屉**：终端 / 监控 / 文件页从左边缘右滑，拉出「主机 → tmux 会话 → 窗口」三级树，
  不用先返回主页就能切走。已 attach 的会话切窗口走侧通道 `select-window`，**终端原地换窗口，
  不重建、不重连、滚屏历史保留**；切到别的会话才新建终端。
  抽屉是**只读**的——重命名 / detach / kill 仍然只在主页做，一个从终端里划出来的浮层不该能删掉东西。
  数据与主页共用同一个 `HomeViewModel`，不存在第二份会话状态。
  拉开时只对当前主机静默刷新一次，且只肯蹭现成的连接：蹭不到就显示带时间戳的缓存，
  **绝不为渲染一个抽屉去拨号**（文件页常常属于这种情况）。
  终端正文区是 AndroidView，会吃掉所有触摸事件，边缘手势因此在祖先节点的 Initial pass 上兜底，
  不到阈值绝不消费，终端的点击 / 选词 / 滚屏一律不受影响。

## [0.1.2] - 2026-08-14

### Changed

- **终端与文件编辑器改用内置的 JetBrains Mono**（OFL 1.1，Regular 单字重，原样打包 268KB）：
  系统等宽字体在 Android 上是 Droid Sans Mono，**不含制表符**（U+2500 系列），htop、vim、tmux
  的框线只能跨字体回退，宽度与格宽对不上就被渲染器横向拉伸，线条歪斜；它字面也偏小
  （x-height 0.53em vs 0.55em），同样 0.6em 格宽里 `l`、`i` 只剩一根光竖线，密排时发虚。
  粗体照旧走 fakeBold，所以只打包一个字重；CJK 仍走系统字体回退

## [0.1.1] - 2026-08-13

### Changed

- **应用内更新改走 cnb.cool**：发布、签名 APK 本来就都在 CNB，检查更新却查着一个不存在的
  GitHub 仓库，「检查更新」从来没成功过。现在设置页一路走完检查 → 下载 → 校验 → 安装，
  下载走 Release 附件直链（匿名可读，不需要任何 token）
- 「最新版本」按版本号从发布列表里挑，不依赖列表顺序，也不看 `is_latest`——
  CNB 上只有一个发布时该字段也是 `false`。草稿与预发布版一律不推给普通用户
- 默认主题改为深色，不再跟随系统：终端本身是白字黑底，外壳跟随系统会在白天变成亮色标题栏配纯黑终端

### Fixed

- **终端连上之后一输入就提示「连接已断开」**：键盘弹出会让 `TerminalView` 改尺寸，同步窗口尺寸的
  SSH 包被发在主线程上，写 socket 失败时 sshj 的包序号已经加过了，序号一错位服务端立刻断连
- **主机列表收起主机卡片会退出 App**：收起时释放侧通道连接，`SSHClient.close()` 在主线程上发
  SSH_MSG_DISCONNECT，抛出的 `NetworkOnMainThreadException` 是 `RuntimeException`，
  sshj 的 `closeQuietly` 接不住，直接把进程带走。退出文件页、关会话走的是同一类路径，一并修掉
- 亮色系统上强制深色主题时状态栏、导航栏图标看不见
- README 徽章与「项目地址」入口指向的 GitHub 仓库并不存在，改指 CNB

## [0.1.0] - 2026-08-13

首个版本。Android SSH 客户端，主页即 tmux 会话切换器。

### Added

**tmux 会话切换器（主页）**

- 三级树：主机 → tmux 会话 → 窗口，就地展开
- 缓存优先渲染：冷启动直接显示上次探测到的会话树并标注快照时间，不发起任何网络请求
- 懒探测：展开某台主机时才拨连接；已有活连接则复用其 `exec` 侧通道
- 会话操作：attach（`new-session -A -s`，存在则附加）/ 新建 / 重命名 / 断开其他客户端 / 结束
- 窗口操作：列出、切换（按 `@id`，不按会移动的 index）
- 非 tmux 的裸会话与 tmux 会话平级挂在主机节点下
- 所有 tmux 动作走带外 `exec` 侧通道，不向前台终端注入按键

**终端**

- 基于 vendored termux 终端内核的 SSH 终端，多会话常驻于 Application 级 `SessionManager`
- 前台服务保活，切后台后会话与传输继续运行
- 断线自动重连：指数退避 + 网络恢复立即重试，重连保留 emulator 与滚屏历史
- 重连后自动 attach 回原 tmux 会话
- 复制 / 粘贴、捏合缩放、滚屏历史
- 附加键栏：`Esc` `Tab` `Ctrl` `Alt` `↑↓←→` `-` `|` `~` `/`，`Ctrl`/`Alt` 支持粘滞

**主机管理**

- 主机增删改：名称、host、端口、用户、分组
- 密码与私钥（PEM）认证，凭据经 Android Keystore（AES-GCM）加密存储
- Keystore 密钥失效（改锁屏方式、恢复出厂、跨设备恢复备份）导致凭据解不开时，
  保住主机的其余配置并明确提示重填，不伪装成认证失败也不重试
- 主机密钥 TOFU 校验：首次记录指纹，变更时阻断式告警并要求显式信任
- ProxyJump 跳板机
- 登录后自动执行命令

**主机监控**

- 发行版 / 内核 / 主机名 / 运行时长 / 负载 / CPU（总体 + 每核）/ 内存 / 交换 / 磁盘 / 网卡 / 进程 Top
- 一次 `exec` 采全量，以直读 `/proc` 为主，采不到的项标「不可用」而非填 0
- 在页面时每 5 秒刷新，离开立即停止轮询
- 仅支持 Linux；无 `/proc/stat` 的主机明确提示暂不支持

**文件管理（SFTP）**

- 浏览 / 上传 / 下载 / 新建目录 / 重命名 / 删除，复用同一条 SSH 连接
- 小文本文件在 app 内编辑保存，二进制与超大文件拒绝打开
- 传输走 Application 级后台队列，进度可见、可取消

**应用内更新**

- 手动检查 GitHub Releases，无后台轮询
- 安装前校验包名、版本确实更新、签名与已安装应用一致，任一不过即删除下载文件

**基础**

- 中英双语，跟随系统，设置内可切
- 亮色 / 暗色 / 跟随系统主题
- 3 套终端配色：默认（xterm）、Solarized Dark、Gruvbox Dark
- 终端字号可调
- 关于页展示 MIT / Apache-2.0 双许可结构与第三方组件清单

**工程**

- MIT 许可；`terminal-emulator/` 与 `terminal-view/` 为 vendored 的 Apache-2.0 模块，
  改动均标注 `[lightmux]` 并在 `NOTICE` 中列明
- 依赖树零 GPL / LGPL / AGPL，APK 内不含任何 GPL 代码
- 单元测试：app 纯逻辑 205 个，vendored 内核上游测试 138 个原样保留
- `scripts/check_strings.py` 校验双语文案的 key 与格式化占位符一致性
- CNB 流水线：main push / PR 跑文案校验 + 单测 + release 构建，tag 出签名 APK 挂 Release。
  日常与发版共用 `test assembleRelease` 一条路径，避免 lintVital 只在打 tag 时才跑到
- GitHub Actions 同样跑构建 + 单测 + 文案校验（开源门面）

### Notes

- **不含 mosh**：mosh 是 GPLv3，集成后分发 APK 须随附源码，对 MIT 项目是持续负担。
  `TerminalTransport` 保留扩展位，将来若做也是独立可选下载，不进主 APK。
- **不内置字体**：终端使用系统等宽字体，CJK 走系统回退。相比 PRD 原计划的内置
  JetBrains Mono，省掉约 1MB 体积与 OFL 保留字体名称条款的合规讨论。

[Unreleased]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.59...main
[0.1.59]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.58...v0.1.59
[0.1.58]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.57...v0.1.58
[0.1.17]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.16...v0.1.17
[0.1.16]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.15...v0.1.16
[0.1.15]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.14...v0.1.15
[0.1.14]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.13...v0.1.14
[0.1.13]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.12...v0.1.13
[0.1.12]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.11...v0.1.12
[0.1.11]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.10...v0.1.11
[0.1.10]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.9...v0.1.10
[0.1.9]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.8...v0.1.9
[0.1.8]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.7...v0.1.8
[0.1.7]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.6...v0.1.7
[0.1.6]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.5...v0.1.6
[0.1.5]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.4...v0.1.5
[0.1.4]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.3...v0.1.4
[0.1.3]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.2...v0.1.3
[0.1.2]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.1...v0.1.2
[0.1.1]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/compare/v0.1.0...v0.1.1
[0.1.0]: https://cnb.cool/ninvfeng/lighttools/lightmux/-/releases/v0.1.0
